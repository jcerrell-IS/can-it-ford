# LS6 session log, 2026-08-12
Note on times: entries below are wall-clock CDT on this node. The Vista log's own timestamps run ahead of this clock (its file was created 14:33 yet carries entries up to 14:55), so Vista entries are cited by label, not treated as clock times.

Node c301-003, job 3360948 (gpu-a100-dev), 2h limit, started 14:05:54, ends 16:05:54 CDT.
Driving shell is login1; the A100 allocation was not needed, item 1 is pure scripting.
Parallel Vista session: job 906873 on c642-031, ends 15:59:24.

## 14:26 | main, session open: squeue confirms job 3360948 running on c301-003, 1:39 left at start. Note the shell is on login1.ls6, not on the compute node.

## 14:29 | main, root disambiguated: two live copies exist. /scratch/11603/jcerrell0629/can-it-ford is stale (clean tree, HEAD f4850bc 2026-07-23, newest file 2026-07-24). The live August repo is /work/11603/jcerrell0629/vista/can-it-ford (HEAD b00bf7b 2026-07-25, tree dirty, .git touched today). All item 1 work was done against the /work/vista copy. Vista's /work is reachable from LS6 via shared Stockyard, confirmed by hashing truck_trimmed.ply from this node.

## 14:30 | main, doc preflight FAILED, same finding as Vista 14:30 independently: docs/REMEDIATION_PLAN_AUDIT_2026-08-12.md does not exist under /home1, /work (ls6, vista, frontera, stampede3), or /scratch, and has never existed in git history on any branch of either repo. The mandatory first read could not be done. Also absent everywhere: params_check.py, analysis/enhance_hero.py, docs/semi_empirical_baseline_findings.md, data/semi_empirical_baseline_2026-08-08.csv, renders/multigeom_2026-08-12_render/, and any "G10a" register reference.

## 14:31 | main, item 1 PREMISE CORRECTED, this is the load-bearing finding. The audit's claim that five fields are "missing from all 32 checked run manifests" is a key-naming artifact, not a provenance hole, and the count is wrong. There are 29 JSON run manifests (data/coupling_validation*/**/*.json), not 32. Census by exact key name: all six audited names absent 29/29. Census by value: solver_git_sha is present 29/29 as provenance.pinned_sha = 544c93dd02cb9c7ead89e1155a62967243244fce (the "known solver SHA" from the brief was already recorded on every run). grid_density present 29/29 as geometry.n_grid (64 or 96). vehicle_mass present 29/29 as geometry.box_mass_kg (1914.28, except c3_fixed2 at 3190.46). So 3 of 5 were never missing; the checker looked for names the writer never wrote.

## 14:32 | main, item 1 bulk_modulus resolved as fully reconstructable, not partly missing. The brief said missing from 3; it is absent from all 29 by name, and recoverable exactly on all 29: solver sets c = sqrt(gamma*bulk/rho) so bulk = c^2*rho/gamma. From geometry.sound_speed 12.84523257866513 with RHO_W 1000.0 and GAMMA 1.1 (validate_coupling_force.py:18-22) this returns 150000.0 Pa, matching the script constant BULK = 1.5e5 exactly.

## 14:33 | main, item 1 mesh_sha256 is INAPPLICABLE, not backfillable. The brief said mesh sha256 values are "already in vehicle_geometry_research/"; they are not, that directory holds zero sha256/checksum records. More decisively, these 29 runs load no mesh at all: build_box_sdf() calls cube_mesh(length) then build_sdf(), and the writer contains no .ply/.obj/.stl reference. Recording a vehicle-mesh hash against these runs would be a fabricated provenance record, so the field is written null with a reason.

## 14:34 | main, item 1 REAL reproducibility gap found, worse than the field naming and not mentioned in the brief: simulation/validate_coupling_force.py, the script that produced all 29 manifests, is UNTRACKED in git (?? in status). The repo's last commit is b00bf7b dated 2026-07-25 while every run dates from 2026-08-07, and the tree carries 46 dirty paths. So no commit SHA can identify the code that ran, and stamping a bare canitford_git_commit would overstate reproducibility rather than establish it. Its content sha256 is the only durable pointer: 2e437a293e2cfd17...

## 14:35 | main, item 1 DELIVERED, three new files, no existing file edited. analysis/run_provenance.py is the forward-going capture module: collect_run_provenance() emits a provenance_v2 block carrying all six audited names with a per-field source and a confidence marker from {measured, derived, recorded, inapplicable, unknown}, plus repo state, run-script hash, and node/SLURM/version environment. Self-test passes on this node. analysis/backfill_run_provenance.py back-fills existing manifests, dry-run by default. analysis/PROVENANCE_V2_INTEGRATION.md plus analysis/_provenance_v2_snippet.py carry the verified writer patch.

## 14:38 | main, item 1 backfill RUN against all 29 manifests, non-destructively. Resolution: grid_density measured 29/29, vehicle_mass measured 29/29, solver_git_sha measured 29/29, bulk_modulus derived 29/29, mesh_sha256 inapplicable 29/29, canitford_git_commit inferred 29/29 (low confidence, from manifest mtime, flagged unreliable). Wrote 29 *.provenance.json sidecars plus data/provenance_backfill_report_2026-08-12.json. Deliberately did NOT use --in-place: the manifests are shared data and the Vista session is live, so the manifests themselves are untouched. --in-place is available (with .bak-preprovenance backups) whenever the user wants it.

## 14:39 | main, item 1 forward-going patch verified but NOT applied, deliberately. The hook belongs at validate_coupling_force.py:1062 (`res["provenance"] = PROVENANCE`, single occurrence), but Vista's log records an edit to that same file (its 14:44 entry) and a .vista_conflict_backup_2026-08-11/ already exists, so an LS6 edit risked a lost-update collision on Vista's item 5 file. The patch was applied to a copy instead: compiles clean, the documented apply procedure reproduces it, the re-apply guard correctly refuses a second run, and the exact call was executed against a real manifest and returns a correct block. Not verifiable on LS6: an end-to-end writer run, since warpmpm is not importable on this node.

## 14:40 | main, item 2 BLOCKED, not started. The brief requires reading docs/semi_empirical_baseline_findings.md first; it does not exist, nor does data/semi_empirical_baseline_2026-08-08.csv, so the "experiment already ran" premise cannot be confirmed from any artifact and there is nothing to anchor a Xia 2011 / Shu 2011 citation search to. No literature search was run, since fetching publisher PDFs for coefficients whose local baseline cannot be located would produce citations with nothing to check them against. Open: user supplies the findings doc or confirms the search should proceed unanchored.

## 14:40 | main, item 3 GATED as briefed, correctly not started. Gate is a reported stall on Vista's item 5 (force coupling). Vista's log (latest entry, labelled 14:55) shows it working item 3 with item 5 not yet reached and no stall reported, so the gate is closed. Also both named sources are absent: citations/GNN_Surrogates_for_Fluid-Rigid_Coupling does not exist, and no graph-neural-network granular-flow paper is in citations/. Nothing trained, nothing read.

## 14:40 | main, item 4 BLOCKED, nothing to post-process. renders/multigeom_2026-08-12_render/ does not exist (renders/ holds only mpm-engine-out/), analysis/enhance_hero.py does not exist anywhere, and Vista's 14:53 entry independently records that the real multigeom run at /work/11603/jcerrell0629/vista/render_s2/multigeom_2026-08-08/ contains metrics.csv + rollout.npz + summary.json only, with no rendered frames ever produced. Enhancement of Vista output cannot begin until Vista produces frames.

## 14:47 | main, items 2-4 REOPENED on user instruction to push all of them as far as they go. The 14:40 blocked/gated entries above stand as the state at that time; what follows supersedes them.

## 14:53 | main, GATE RESOLVED for item 3, and NOT in the direction the brief expected: the Vista session has ENDED and its item 5 did not stall. Its log records item 5 IMPLEMENTED (simulation/coupling_force/) with test_rigid_body.py passing 14 analytic checks, plus a correction that sdf_wrench() at core/solver.py:354 already returns force and torque, making item 5 "far cheaper than budgeted." What is open is Vista's item 6, the first coupled run, needing ~1 GPU-hour and a fresh allocation. That is an allocation gap, not a stall. Vista's clock runs ~35 min ahead of this node's, so its entry labels are not comparable as wall-clock times.

## 15:00 | main, item 2 DONE to the limit the paywalls allow, written to docs/SEMI_EMPIRICAL_BASELINE_CITATIONS_2026-08-12.md. Both papers identified. Shu 2011 = Shu, Xia, Falconer & Lin, J. Hydraulic Research 49(6):709-717, DOI 10.1080/00221686.2011.616318, exact title match. "Xia 2011" resolves to Xia, Teo, Lin & Falconer, Natural Hazards 58(1):1-14, DOI 10.1007/s11069-010-9639-x, which this repo's own bib already keys xia2010: online-first 2010-10-20, print volume 58 is the 2011 volume, so "Xia 2010" and "Xia 2011" in this project are the SAME paper, not two sources. Two unrelated genuine Xia 2011 papers exist (Proc ICE Water Management 164(6) and Env Modelling & Software) and must not be conflated.

## 15:00 | main, item 2 SUBSTANTIVE FLAG: the coefficient-bearing paper is probably Xia 2013/2014 (Nat Hazards 70(2):1619-1630, DOI 10.1007/s11069-013-0889-2, keyed xia2013 here), not either 2011 paper. Its abstract states ~200 flume runs across three orientation angles and that large-scale model data "were then used to determine two parameters in the derived formula", and downstream literature attributes the vehicle formula to "Xia et al. (2014b)". If the baseline needs calibrated coefficients rather than the sliding-equilibrium derivation, the brief may be pointing at the wrong year.

## 15:00 | main, item 2 ACCESS: all three papers are oaStatus closed with content denied. No legitimate free publisher PDF exists, and ZERO full-text excerpts were retrievable, so no coefficient in the new doc is quoted from a full text. ORCA Cardiff holds Shu 2011 at eprint 17057 but returned HTTP 403 to automated fetch (bot protection, not necessarily restriction) so green-OA availability is UNCONFIRMED and needs a browser. The route that actually unblocks this is an authenticated UT Libraries session. academia.edu/ResearchGate copies exist for both and are NOT citation-grade. The brief's precondition therefore still holds: publisher PDFs remain required before citing coefficients.

## 15:20 | main, item 4 DONE and verified, analysis/enhance_hero.py written from scratch (it existed nowhere). Confirmed NON-duplicative: render_s3_enhanced/sim_enhanced.py and run_enhanced.py are ENHANCED-PHYSICS drivers (a bulk-modulus sweep), not image post-processing, and render_rollout.py is the renderer, which this script never touches. Environment measured on LS6: numpy 2.0.2, PIL 10.4.0, scipy 1.6.1 present; cv2, imageio and ffmpeg ABSENT and matplotlib BROKEN (compiled against numpy 1.x, raises _ARRAY_API not found under numpy 2.0.2). So the tool depends on PIL+numpy only and writes an animated GIF via PIL instead of mp4, plus a make_mp4.sh for a node that has ffmpeg. NOTE this also means render_rollout.py cannot run on this LS6 node at all.

## 15:20 | main, item 4 tested against real frames, 90 PNGs at 1280x960 RGBA under render_s3_enhanced/mpm_render_iqlwpeq5/. Verified: dry-run writes nothing; all three write guards return exit 2 (out == input, out nested inside input, --out missing); input directory confirmed untouched at 90 files; full run wrote 30 frames + 4 hero stills + contact sheet + GIF in 20 s; re-run correctly skips existing. Found and fixed a real defect in passing: a no-op re-run overwrote enhance_manifest.json with an empty record, destroying the provenance the manifest exists to keep. Now preserved unless --force.

## 15:25 | main, item 4 IMPORTANT finding from actually looking at the frames: the existing render_s3_enhanced PNGs are NOT photorealistic hero renders, they are matplotlib DIAGNOSTIC plots, white background, viridis particle scatter, labelled axes, plus a local-depth time-series panel. Enhancing them with the photo presets would remap a colormap, so the value a reader decodes from a colour would no longer be the value the solver produced, i.e. data misrepresentation. Added a guard: frames averaging >200/255 with a colour-altering preset now emit an explicit WARNING recommending --preset none (assembly only: hero selection, contact sheet, GIF). The photo presets remain correct for the photorealistic renders Vista has not produced yet.

## 15:35 | main, item 3 ASSESSED, nothing trained, written to docs/GNN_SURROGATE_ASSESSMENT_2026-08-12.md. Verdict: a GNN surrogate is NOT a faster path to visual realism than the coupled solver. The real paper is the PI's own: Choi & Kumar 2024, Computers and Geotechnics 166:106015, DOI 10.1016/j.compgeo.2023.106015, CC-BY, arXiv 2305.05218v2. Figures verified against the full PDF text via pdftotext, not an abstract: speedup 369-740x (2D GPU) and 1819-2867x (3D GPU) against CB-Geo MPM on 56 Frontera cores, GNS rollout on one A100 40GB on LS6; training data only 26 trajectories (column collapse) and 890 (barriers); training duration 15 days (2D) and 22 days (3D); GPU ceiling ~439K material points at 40GB; barriers are FIXED rigid bodies; DRY GRANULAR ONLY, no fluid coupling anywhere in the paper.

## 15:35 | main, item 3 CORRECTION WORTH KEEPING: an automated extraction of that same PDF returned "~4,000x speedup", "10,000 training trajectories" and "7 days on a single V100". All three are WRONG, caught by extracting the PDF text directly. Do not cite those numbers; the table above is the verified set.

## 15:35 | main, item 3 reasoning: (a) circular, a surrogate trains on trajectories from the very solver it replaces, and this project cannot yet produce even one validated coupled run since Vista item 6 never executed; (b) wrong physics, fixed barriers vs the free 6-DOF buoyant body that is the actual unsolved problem; (c) cost inverted, 15-22 days training vs ~1 GPU-hour for item 6, and the speedup is amortized inference that does nothing for one hero render; (d) the accuracy tradeoff points the wrong way, since the current defect IS physical (rho~300 bodies not rising, hull passthrough ~0.098) and a surrogate would reproduce a car that refuses to float, faster, with explicit-Euler error accumulation on top. Constructive: GNS fits the depth-velocity SWEEP once a validated corpus exists (the project already does a cheaper GP version), and the correct fluid-capable reference is Kumar & Vantassel 2022, arXiv:2211.10228, not the granular paper.

## 15:40 | main, RECOMMENDATION at session end: the single highest-value next action is Vista's item 6, the ~1 GPU-hour first coupled run. It is the blocker for physical correctness, for visual realism, and for any future surrogate corpus. It could NOT be run from this LS6 session: warpmpm is not importable on this node (ModuleNotFoundError), so it needs the Vista/GH200 environment. Also still open from item 1: simulation/validate_coupling_force.py remains UNTRACKED in git, which is the actual reproducibility gap and a one-line fix.

## 15:08 | main, item 1 SALib INSTALLED, SALib 1.5.2 via pip --user. SIDE EFFECT WORTH KNOWING: it also pulled matplotlib 3.9.4, scipy 1.13.1, pandas 2.3.3 into ~/.local, which REPAIRS the broken system matplotlib recorded at 15:20 earlier (the numpy 1.x/2.0.2 ABI break). That means render_s3_enhanced/render_rollout.py may now be runnable on LS6 where it previously was not. This changed the shared user environment, not just this session. A noisy non-fatal numexpr _ARRAY_API warning remains (system numexpr is numpy-1.x compiled, pandas falls back cleanly).

## 15:12 | main, item 1 STRUCTURAL FINDING that reframes the whole screen: the four requested factors are NOT the same kind of parameter. floor_friction, grid resolution and timestep are SIMULATION inputs, so evaluating them requires the solver and warpmpm is not importable on LS6, i.e. they are GH200 work. DRIFT_THRESHOLD is NOT a simulation input at all: verdict = "NO-FORD" if max_x_disp > DRIFT_THRESHOLD (can_it_ford_L2_mpm.py:189 and :258), so it is a post-processing cut on a displacement the run already produced. Its effect is therefore exactly computable with no GPU and no sampling error, which is what I did.

## 15:15 | main, item 1 THE RESULT THAT SHOULD CHANGE VISTA'S PLAN: the binary FORD/NO-FORD verdict is very nearly CONSTANT on all existing data, so a Morris screen against it would return mu*=0 for every factor as an artifact of the metric, not a property of the physics. Verified over 72 pooled runs (track1_sweep_v1 + v2, final_disp_m): at DRIFT_THRESHOLD=0.05 only 2/72 are FORD (0.028). gp_surrogate_metrics.json independently records the same degeneracy from its own 33-run subset: "threshold": 0.05, "n": 33, "n_ford": 0, "ford_runs": []. Recommendation: run Morris on the CONTINUOUS QoI max_x_disp_m (ideally log10, it spans 0.0197 to 2.4052 m, about two decades), and drop DRIFT_THRESHOLD from the design entirely since its effect is exact and needs no runs. That cuts the screen from k=4 to k=3, i.e. r*(3+1) instead of r*(4+1) runs.

## 15:15 | main, item 1 DRIFT_THRESHOLD sensitivity QUANTIFIED, the uncited 0.05 m is now an analyzed number. Over 72 runs the FORD fraction spans 0.014 to 0.431 as the threshold moves 0.02 to 0.20 m, a swing of 0.417. Centred difference at the operating point 0.05 m with +/-0.01 m gives d(FORD fraction)/d(threshold) = 3.47 per m, i.e. a 1 cm change in this constant moves the FORD fraction by 6.9 percentage points. So DRIFT_THRESHOLD is a DOMINANT control on the reported verdict, and the project's existing framing of it as "a conservative numerical onset-of-motion detection tolerance, not a peer-reviewed criterion" is not just defensible, it is necessary: at 0.05 m the verdict sits on the steep part of the curve, just above the displacement floor (min observed 0.0197 m).

## 15:20 | main, item 1 HARNESS DELIVERED and self-tested, analysis/morris_screen.py. Implements Morris (1991) elementary effects with the Campolongo, Cariboni & Saltelli (2007) improvements (mu* and optimal-trajectory selection), both cited in the module docstring with DOIs. Four subcommands: --design (emit the run list), --analyze (ingest completed runs, report mu/mu*/sigma), --threshold-scan (the exact DRIFT_THRESHOLD result above), --self-test. SELF-TEST PASSES: on f = 10a + 3b + 0.5c + 0d the harness recovers mu* = 10.0000, 3.0000, 0.5000, 0.0000 exactly and the correct ranking, and the constant-QoI guard fires. That guard REFUSES to analyze a zero-variance QoI rather than reporting all-zero effects, which is precisely the trap the binary verdict sets.

## 15:25 | main, item 1 DESIGN BUG found and fixed in passing, worth recording because it would have silently wasted the expensive runs. Sampling grid_resolution directly over [48,96] and snapping to {48,64,96} puts a Morris grid point at 80, equidistant from 64 and 96; the tie broke low and STARVED the expensive g96 level, measured 48/64/96 = 22/23/5 of 50 runs. Fixed by sampling in index space [0,2] and rounding. Level balance is now reported on every --design call so it can never be silent again. Empirically --levels 6 balances a 3-level discrete factor best: 25/18/17 versus 22/29/9 at levels 4 and 25/28/7 at levels 8. Note --levels 3 is WRONG here despite matching the level count: Morris's delta then visits only the extremes, giving 30/0/30 with g64 never sampled.

## 15:28 | main, item 1 HANDOFF TO VISTA: data/morris_design_k4_r12.csv, 12 Campolongo-optimal trajectories, 60 evaluations, factors floor_friction [0.25,0.75] (project's own documented mu_wet sensitivity range, current value 0.55), grid_resolution {48,64,96}, dt_scale [0.25,2.0] as a multiplier on the nominal CFL timestep, drift_threshold [0.02,0.20]. Vista fills max_x_disp_m per row then runs --analyze. STRONGLY SUGGEST regenerating without drift_threshold first (k=3, 48 runs at r=12) since its effect is already exact, and using --log-qoi. Flagged per the brief: all 60 evaluations need full GH200 physics, none of them can be approximated here, and no fast surrogate covers these factors (analysis/gp_surrogate.py is trained on depth/velocity/mass/bbox only, not friction, grid or dt).

## 15:31 | main, REALISM TRACK opened at realism_track/, separate from renders/yaris_render_s1/ which was not touched (git status on that path is empty). Mandatory reading: 2 of 4 docs do not exist anywhere (CANONICAL_CORRECTIONS_REGISTER_2026-08-06.md and MULTIGEOM_VALIDATION_2026-08-11.md, verified across all roots), COUPLING_VALIDATION_J1_2026-08-07.md exists only as the _VISTA_ variant, CLAUDE.md read in full.

## 15:31 | main, THE BRIEF'S 7.3-7.7% IS CONFIRMED from primary data, register not needed. Re-derived from the 29 coupling_validation manifests: c1sdf_sdf_g64 err_steady_vs_analytic_pct = -7.67, c1sdf_sdf_g96 = +7.28. The brief's warning about 1.6% is also confirmed: 1.62 is err_first3_vs_analytic_pct, a first-three-frame transient, not the steady figure. Full ordering, SDF collider 7.3-7.7% beats box collider 21.28-37.91% beats material-8 free-rigid 48.79-345.05%, which is direct quantitative confirmation that the free-rigid path exchanges no real force.

## 15:31 | main, SMOKE RUNS EXCLUDED with cause, not convenience: at IDENTICAL geometry (zero geometry-key diffs vs c1sdf_sdf_g64) the smoke run's F_steady_tail_mean is -9628.7 N versus the gated run's +28898.4 N, i.e. sign-inverted, with a_free_body_implied -14.84 m/s2 (below free fall), 8x the lateral asymmetry (F_lateral_over_Fz 0.320 vs 0.040) and a different settle state. Pooling them would give a misleading 7.28-130.76% range. The settle gate is load-bearing.

## 15:31 | main, MOVING-SDF QUESTION ANSWERED YES: core/solver.py:339 set_sdf_pose accepts center/quat/velocity/omega, alongside add_sdf_collider :324, reset_sdf_force :348, sdf_wrench :354. So reset -> step -> wrench -> Newton-Euler -> set_sdf_pose gives the validated force AND freedom of motion; the "a fixed collider cannot slide" objection was an artifact of how the collider was driven, not an engine limit. ALREADY IMPLEMENTED by Vista in simulation/coupling_force/ (rigid_body.py, rung_b_coupled.py, inflow_outflow.py + tests), so NOT duplicated. Path (b) Genesis not pursued: strictly more work for a less-validated force, since no coupling-force validation exists anywhere in the Genesis repo and path (a) already has one.

## 15:32 | main, RENDERS NOT DELIVERED and no visual-realism claim made. Toolchain measured absent on LS6: splashsurf, blender, mitsuba, pvpython, ffmpeg all absent; pyvista, trimesh, skimage, open3d, warpmpm, genesis, taichi all not importable. So neither the meshing/rendering half nor the coupling experiments can run on this node. drainA PLY verified intact (270,857,262 B). Vista's multigeom mp4s now exist (rogue 5,468,214 B, silverado 4,496,217 B) but are kinematic-path outputs predating any real coupling force and must not be presented as coupled results.

## 15:33 | main, COMMITTED 1e4c6d5 with explicit paths only (realism_track/FINDINGS.md, verify_coupling_accuracy.py, coupling_accuracy.json). No git add -A. Vista's three dirty tracked files left unstaged and untouched. Not pushed. Two canonical conflicts flagged in FINDINGS for a human decision: CLAUDE.md attributes physical mu 0.3-0.55 to "Azhar et al. 2023" while the project's own audit recorded that attribution as UNVERIFIED, and the brief's three-vehicle density corrections contradict CLAUDE.md Section F's "one usable mesh, not three" (so the 310.5->241.1 / 317.4->257.7 / 285.1->245.1 values were NOT verified and are used nowhere).

## 16:22-17:07 | main, RUNG B COUPLED RUN EXECUTED, on LS6 A100 not Vista GH200. Vista is UNREACHABLE from LS6: ssh returns "Permission denied (keyboard-interactive)", TACC MFA cannot be satisfied non-interactively. Rather than stop, moved the run to LS6, which is where the realism brief's own resource guidance puts warpmpm SDF-collider work. Zero Vista SU spent, 670 SU preserved. Installed warpmpm deps into /scratch/11603/jcerrell0629/warpmpm_ls6_env (warp-lang + torch + numpy, 6.7 GB, 24 min on BeeGFS). LS6 is x86_64 so the known aarch64 failure class does not apply. Jobs 3361315 (baseline g64+g96) and 3361371 (relax sweep), node c301-004, 3x A100-PCIE-40GB.

## 17:07 | main, ITEM 1 ANSWERED, and the answer is NEGATIVE: the moving collider does NOT preserve the fixed-collider accuracy. g64 goes -7.67% -> -18.86%, g96 goes +7.28% -> +115.03%. The brief's instruction not to inherit 7.3-7.7% was correct and is now empirically confirmed. Force IS real on this path (nonzero_wrench true, clamped_steps 0 everywhere, 445,184 particles at g64 and 1,502,496 at g96), and the 14 analytic integrator tests passed on GPU matching Vista's CPU run (SHM 1.73731 vs theory 1.7373 s).

## 17:07 | main, INSTABILITY HYPOTHESIS REFUTED by the relax sweep. Both runs tripped the coupler's own added_mass_ratio guard (0.864 at g64, 0.930 at g96, limit 0.5), making partitioned-explicit instability the obvious suspect. Relax 1.0 -> 0.5 -> 0.25 made the force error monotonically WORSE (-18.86/-19.94/-23.12 at g64; +115.03/+119.91/+132.28 at g96) while added_mass_ratio stayed fixed, since it depends on geometry and density ratio not the scheme parameter. Relaxation damps the motion response (g96 a_first3 39.85 -> 20.48) without improving the wrench. So the error is systematic, not oscillatory divergence.

## 17:07 | main, g96 IS UNPHYSICAL and the refinement behaviour points at a bug: a rho_box=600 body in rho=1000 water must sink at a_ideal=-0.689 m/s2, but it accelerates UPWARD at +39.85 m/s2 (4x gravity) and rises 0.40 m. Error worsens AND flips sign from g64 to g96, which convergent schemes do not do. Most plausible causes: double-counting between set_sdf_pose and the grid velocity update, or a sign/frame error in wrench application. CONCLUSION: the moving-SDF path is NOT validated and must not be presented as such; the fixed collider remains the only configuration inside the 5-10% target. Cheapest next diagnostic, one run: hold the body fixed through the SAME new code path (wrench read via sdf_wrench, pose never updated). Reproducing -7.67% at g64 isolates the defect to the pose-update loop; not reproducing it isolates the defect to the wrench read.

## 17:07 | main, ITEM 2 sound speed NOT RUN, two reasons. Practical: rung_b_coupled.py exposes no bulk/sound-speed argument at all, it inherits BULK=1.5e5, so c=1480.98 needs a code change not a flag. Required bulk is 1.9939e9 Pa (13,293x current) and by the solver's own CFL rule substeps go 11->1198 at g64 and 16->1797 at g96, about 109-112x more compute. Evidential and stronger: item 1 just showed the model is wrong at both resolutions, so sweeping the project's most expensive parameter on it would spend ~100x a run's cost measuring an artifact. Sound speed must follow a coupling path that reproduces analytic buoyancy, not precede it.

## 17:33-17:47 | main, DIAGNOSTIC JOB 3361423 COMPLETE, and it RETRACTS the pose-loop hypothesis. [READ] Six runs of realism_track/diag_wrench_fixed_pose.py, three modes x g64/g96. pose_full reproduces rung (b) to 0.09 pp (-18.86% vs -18.86% at g64, +114.94% vs +115.03% at g96), so the diagnostic is a faithful stand-in. But the `fixed` mode did NOT reproduce -7.67%: with the body held completely still it read -48.49% at g64 and +349.55% at g96. Both hypotheses stated at 17:07 were therefore wrong. The pose-update loop is not the primary defect and the wrench read is not sound in this configuration.

## 17:47 | main, ROOT CAUSE FOUND, and it is the settle state plus the geometry, not collider motion. [READ] rung_b_coupled.py and run_c1_sdf never shared a configuration. (1) Submersion: run_c1_sdf (validate_coupling_force.py:899-1000) uses box_bottom_cells=8.0, which puts box_top exactly at the nominal free surface, and the reference JSONs show the cube ends FULLY submerged, submerged_height_frac=1.0 with +2.75 dx (g64) and +5.36 dx (g96) of water above the top, scored against rho_w*V*g=31,298.44 N. rung_b calls box_bottom_cells_for_submersion(0.80, 18) and realizes frac 0.5187 against a partial reference of 16,233 N. (2) Settling: settle_pinned (:620-646) advances tank.substeps per iteration (11 at g64, 16 at g96) and exits on a sound_speed/vmax >= 20 gate, met at 354 frames = 3,894 substeps (g64) and 776 frames = 12,416 substeps (g96), ending at vmax 0.510 and 0.634 m/s. rung_b's settle loop advances ONE substep per iteration with no gate, so 900 substeps is 23% of the reference settling at g64 and 7.2% at g96. tank.pin() is a documented no-op for collider modes (:365-367), so the pin is not a third difference.

## 17:47 | main, the SCATTER confirms the tank is still ringing. [READ] Reference tail std 828 N at g64 (2.9% of mean) and 102 N at g96 (0.3%). In rung (b)'s configuration Fz sweeps -3,360 to +23,847 N at g64 with the body FIXED, peak-to-peak 27,207 N against a 16,233 N analytic, i.e. 1.68x the quantity being measured; g96 sweeps 35,077 to 118,831 N. settle_vmax_peak in the reference runs is 9.82 and 12.43 m/s against c=12.845 m/s, so the initial collapse is nearly sonic and needs the full gated settle to decay. [INFERRED] My 17:07 headline "the moving collider does not preserve 7.3-7.7%" is true as an observation but WRONG in attribution: it charged the gap to motion when most of it is geometry and settle state. FINDINGS.md now marks that section superseded on cause, numbers retained.

## 17:47 | main, ONE GENUINE POSITIVE about the API. [READ] pose_zero_vel vs pose_full isolates set_sdf_pose's velocity argument: g64 moves -45.53% -> -18.86% with net descent halving from -0.2449 m to -0.1236 m, g96 moves +62.33% -> +114.94%. Both shifts are toward more upward reaction, so the velocity argument does reach the grid coupling and is not silently dropped. That is a prerequisite for the moving-collider path, now confirmed rather than assumed.

## 17:52 | main, wrote realism_track/rung_b_settled.py and submitted JOB 3361443 (gpu-a100-dev, PD on Resources at submit). It runs the REFERENCE geometry through both code paths: modes fixed and coupled, g64 and g96, box_bottom_cells=8.0, settle_pinned WITH the convergence gate, scored on the same steady-tail-mean statistic against rho_w*V*g. cube_inertia is imported from rung_b_coupled so the inertia tensor is identical and geometry plus settling are the only changes. Success looks like the fixed arm reproducing -7.67% and +7.28%, which validates the harness and makes the coupled arm the first honest moving-collider measurement. Most likely failure mode: g96 needs 12,416+ settle substeps and the four runs may not all fit the 2 h dev window, so the g64 pair is ordered first. Any run failing the gate is flagged settle_is_discard in its JSON and must not be quoted. COMMITTED 20e2063, explicit paths only.

## 17:52 | main, NOTE a foreign job in the queue: 3361441 "issplat_" on partition gpu-a100, PD, not submitted by this session. Left alone.

## 18:05 | main, ITEM 2 RE-COSTED against the gated settle, and it is out of reach, not merely expensive. [READ] Reproduced substeps_and_dt with the true LIM=9.421742313727737: 11 substeps at dt 3.030e-3 (g64) and 16 at 2.083e-3 (g96), matching every run in this track, so the cost model is validated before extrapolation. At bulk 1.9939e9 (c=1480.98) substeps go 11->1198 and 16->1797, factors 108.9x and 112.3x. Applied to the settle lengths the reference runs actually needed: 3,894 -> 424,092 substeps at g64 and 12,416 -> 1,394,472 at g96. At the throughput measured in job 3361423 (~11 substeps/s g64, ~8.5/s g96 excluding startup) that is ~11 h and ~46 h of settle PER RUN before any measurement substep, and the g96 figure exceeds a normal LS6 single-job wall clock.

## 18:05 | main, TRAP RECORDED: monkeypatching BULK would be worse than having no knob. [READ] sound_speed() and substeps_and_dt() take bulk as a DEFAULT ARGUMENT bound at def time, and BoxTank calls both with no argument (:267-268), while set_material reads the module global at call time (:272). So reassigning BULK stiffens the fluid but leaves dt and the reported sound speed at 12.845 m/s: the run is under-resolved ~110x AND the settle gate (sound_speed/vmax >= 20) is evaluated against the wrong speed, passing far too early. A correct knob must thread bulk through BoxTank.__init__ to all three call sites. NOT edited now, deliberately: job 3361443 is reading that file.

## 18:05 | main, LEAD (not a result) that attacks today's root cause: seed the water pre-compressed. [READ] The settle exists only because water is seeded uncompressed, J=1 everywhere, no pressure gradient at t=0, so the column collapses into its own hydrostatic profile with settle_vmax_peak 9.82 (g64) and 12.43 m/s (g96) against c=12.845, i.e. very nearly shocked at startup. Both pieces needed to skip it already exist: hydrostatic_density(zeta) at :74 already computes the profile for this EOS and is currently used only for reporting in run_c3 (:1008), never at seeding; and the engine exposes import_particle_F_from_torch (kernels/mpm_solver_warp.py), reachable via _sim the same way BoxTank.pin writes sim.rigid_x_cm (:365-372), so NO engine change is needed. Seeding F = J^(1/3)*I with J = rho_w/hydrostatic_density(depth) would start the column at equilibrium compression. Unrun and unmeasured, recorded because it is the only identified route by which a physical sound speed becomes affordable.

## 18:05 | main, JOB 3361443 still PD (Resources) on gpu-a100-dev at 18:05, no output file yet. Monitor armed to report each run's settle gate and force error as they land, and to cover failure signatures (Traceback, CUDA error, OOM, Killed, gate-not-met) so silence cannot be mistaken for progress.
