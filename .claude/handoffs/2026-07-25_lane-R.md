
## LANE R, mesh-render run, 2026-07-25 16:00 UTC

Surface: Mac-local Claude Code, one-shot SSH to Vista. Node c642-012 (reused idle
allocation 865958, verified 2 MiB / 97871 MiB and 0% GPU before use). No job queued.

### New file created (one, as authorized)

`/work/11603/jcerrell0629/vista/mpm-engine/examples/flood_vehicle_meshrender.py`, 193 lines,
untracked. Syntax OK. No taichi/genesis/pyvista/vtk/bpy. No comments, no docstrings.
`examples/flood_vehicle.py` and `src/warpmpm/vehicle.py` NOT edited by this session
(vehicle.py's M predates it). `examples/flood_vehicle_separable.py` left alone.

### Rigid-particle accessor used, quoted so the next session can check

    vehicle.py:302        self.n_water = len(water)
    vehicle.py:303        self.n_total = len(pos)
    vehicle.py:394    def water_positions(self) -> np.ndarray:
    vehicle.py:395        return self.solver.x()[: self.n_water]

The new file scatters the vehicle as `solver.x()[n_water:n_total]`, mirroring :394-395.
It never calls `splat_positions()`, which returns None for a mesh-loaded vehicle.
Confirmed live this run: `splat_colors is None: True`.

### Exact argparse flags of examples/flood_vehicle.py

    --vehicle  --up  --depth  --velocity  --frames  --grid
    --vehicle-density  --vehicle-mass  --out  --device  --no-render

`--n-grid` DOES NOT EXIST, the flag is `--grid`. `--seed` DOES NOT EXIST; the seed kwarg
is reachable only by constructing FloodScene directly (vehicle.py:260). The new file adds
`--seed`.

### Command

    /work/11603/jcerrell0629/vista/.venv/bin/python examples/flood_vehicle_meshrender.py \
      --vehicle /work/11603/jcerrell0629/vista/can-it-ford/vehicle_geometry_research/yaris_coarse_v1l_watertight.ply \
      --up z --depth 0.30 --velocity 1.5 --frames 90 --grid 64 \
      --vehicle-mass 1100 --out out/flood_vehicle_meshrender

### Printed discretization, live, nothing carried from any document

    ext              [1.7461 4.2825 1.5177]
    lim              9.4216 m
    dx               0.147212 m
    h                0.073606 m
    n_water_layers   4
    n_water          23744
    n_vehicle        19327
    vehicle/water    0.8140
    solid_volume     7.7073 m3
    ratio to hull    2.1756
    eta=1.0     acoustic=311.6311 viscous=0.276863 advective=20.3788 rate=311.6311 substeps=11
    eta=0.001   acoustic=311.6311 viscous=0.000277 advective=20.3788 rate=311.6311 substeps=11
    grid 64^3 lim=9.42m water 23744 + vehicle 19327 particles (1100.0 kg) dt=3.03e-03 (11 substeps/frame)

lim=14.99 is REFUTED at T1. The ledger's 9.4217 is confirmed. Layer count steps by h, not
dx, giving 4, not 2. Lowering water_eta from 1.0 to 1.0e-3 does NOT change substeps: eta
appears only inside a max() and acoustic dominates viscous by 1126x. Free, but not changed
this turn.

### Particle counts and volume, the headline defect

n_vehicle/n_water = 0.814. NOT starved, so no solidify_columns hollowing.
solid_volume 7.7073 m3 against the hull's 3.5427 m3 is 2.1756x. The underbody is bridged
shut and buoyancy is OVERSTATED. This independently reproduces ledger A9's n_grid=64 row
(7.698 m3, 2.17x) at T1, closing A9's own "[FLAG] D7: not reproduced on this pass".

### Result

final dx=0.1235 m, dy=0.0027 m, dz=-3.74e-05 m, |d|=0.1235 m, yaw=0.188 deg,
pitch=7.07e-05 deg, roll=0.00218 deg. dz is four orders below dx, consistent with the
restitution rigid-body contact at vehicle.py:313-314 pinning the body vertically, NOT with
buoyancy. |d| = 12.35 cm exceeds DRIFT_THRESHOLD 0.05 m, so this condition is NO-FORD.

metrics.csv header is the 15-column schema:
    t,dx,dy,dz,dmag,yaw_deg,pitch_deg,roll_deg,vx,vy,vz,vmag,wx,wy,wz
Velocity columns ARE emitted by current code, confirming the section 4.4 blocker is stale
data and not a missing capability. failure_modes.py can consume this run.

### Encode: BRANCH A

The in-script encode failed, subprocess.CalledProcessError exit 127, because ffmpeg is
absent from compute node c642-012. CORRECTION to an earlier Phase 1 answer: /usr/bin/ffmpeg
exists on the LOGIN node but CANNOT EXECUTE, it fails with
"error while loading shared libraries: libunwind.so.8". The only working encoder is
/work/11603/jcerrell0629/vista/.venv/lib/python3.12/site-packages/imageio_ffmpeg/binaries/ffmpeg-linux-aarch64-v7.0.2
Encoded separately with that binary at 7 fps.

### Four success criteria

1. mp4 exists and is non-zero: PASS, 435,264 bytes
2. vehicle-shaped cluster visible, not water only: PASS on visible and distinct,
   FAIL on "Yaris-shaped". Frame 22 shows a solid orange body clearly separate from the
   viridis water, but it is a rounded loaf with no windshield rake, wheel arches, or
   roofline. That is the 2.1756x over-fill rendered: column filling bridges the underbody,
   so the envelope is drawn, not the silhouette.
3. metrics.csv non-zero dx, finite yaw/pitch/roll: PASS
4. water layers at least 2 from the printed lim: PASS, 4

### Provenance-audit artifact row

| Field | Value |
|---|---|
| Artifact | out/flood_vehicle_meshrender/flood_vehicle.mp4, 435,264 bytes, 2026-07-25 16:00 |
| Source data | out/flood_vehicle_meshrender/metrics.csv, 34,620 bytes, 2026-07-25 15:55; 45 PNG frames |
| Generating script | examples/flood_vehicle_meshrender.py, UNTRACKED, no commit hash |
| Engine commit | mpm-engine fd390d6, with src/warpmpm/vehicle.py UNCOMMITTED-MODIFIED |
| Params | depth 0.30 m, velocity 1.5 m/s, n_grid 64, vehicle_mass 1100 kg, seed 0, up z, 90 frames, water_eta 1.0 (default), floor_friction 0.5 (default) |
| Track and solver | Track 1, kks32/mpm-engine warp MPM, FloodScene, rigid-body coupling |
| Vehicle asset | yaris_coarse_v1l_watertight.ply, mesh branch (is_gaussian_ply False) |
| What it shows | A 1100 kg rigid body under a 0.30 m / 1.5 m/s dam-break surge drifting 12.35 cm laterally with negligible rotation, water resolved by 4 particle layers |
| What it does NOT show | A recognizable Yaris silhouette. The rendered body is a 2.1756x over-filled envelope, so displaced volume and therefore buoyancy are overstated. It does not show a FLOAT verdict of any kind: dz is pinned at 3.7e-05 m by the restitution contact, not resolved by physics. It is not steady flow; FloodScene applies an impulsive velocity kick after settling, so AR&R D x V thresholds are not directly comparable. |
| Caveat label | UNVERIFIED |

Label rationale: all four success criteria pass, but the instruction was to label MPM-REAL
only if the counts AND the volume ratio are sane. The volume ratio is 2.1756, so it is not.
NOT MPM-REAL. NOT INVALID either: the run is a legitimate, reproducible measurement whose
geometry bias is quantified rather than unknown.

### Named, not guessed

- Whether the 2.1756x over-fill is fixable at n_grid 64 by densifying the 60k surface sample
  before solidify. Untested. mesh.sample(60_000) at vehicle.py:162 is unseeded.
- Whether dz would move at all under a separable floor. NOT tested. Solver.add_plane's
  docstring at core/solver.py:206 states "Grid boundary conditions do not affect rigid
  particles", so the surface string governs water only and the dz suppressor is the
  restitution contact. The slip-vs-separable experiment as designed would not test vehicle
  vertical DOF.
- Node 865958 belonged to another pane. It was idle when reused and was not released by this
  session.
