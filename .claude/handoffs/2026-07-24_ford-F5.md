# F5 handoff: poster asset table

Pane F5, ford:0.5. Written 2026-07-25. Deliverable: `docs/POSTER_ASSET_TABLE.md`, 36 rows.

Read-only. No data regenerated, no simulation, no commit, no push, no GPU, no warpmpm import.
F5 is sole writer of the asset table and touched no other project file. C5's handoff was
folded in; C5 did not touch the table.

## Live corrections to the briefing

1. **Ahead count is 6, not 3.** C5 read 4. Three commits landed during this pass: `60a01a2`,
   `63e677f`, `9f5d82e`. Push hold respected.
2. **`data/scenario_sweep.csv` was rewritten mid-audit**, 8 columns to 10, classes renamed to
   `small_passenger` / `large_passenger` / `large_4wd` plus `L1_class_sensitive`. Seed row 1
   was re-verified against the live 10-column file. Still 70 rows, still 12 class-sensitive.
3. **The ledger's `h < 0.60` inclusivity flag on `analysis/make_phase_space_v2.py:9` is
   RESOLVED.** C5 could not re-check it after the 22:57 edit. F5 did: it now reads `h <= 0.60`
   and line 6 drops duplicate `(depth, velocity)` pairs.

## Addendum applied

- **Seed row 2 reframed.** The 2.30x uncertainty band framing is retracted in the table. It is
  recorded as a one-sided bias: true traction 3495.2 N lies outside the measured range, all
  three resolutions understate by 60 / 30 / 8.3 percent, and the reason a band and a bias are
  not interchangeable is stated in the row itself.
- **Row 4 added, VERIFIED, no blocker.** Track 2 null result. Source qualified as Vista-only at
  `/work/11603/jcerrell0629/vista/can-it-ford/logs/`, explicitly not a local path.
- **Five rows relabelled RETRACTED, not BLOCKED: rows 13, 27, 28, 29, 30.** Those are
  `figures/hero_shot_test.png`, root `phase_space_results_mpm.csv`, the 8 `particles_mpm_*.npz`,
  the 3 Track 2 `.mp4` files, and `data/track2_sweep/manifest.csv`.

## What changed since the mission was written: row 2's blocker is discharged

Another pane produced `analysis/plot_traction_bias.py`, `figures/traction_bias.pdf` and
`figures/traction_bias_CAPTION.md` during this pass, all currently UNTRACKED. They supply the
true-hull-volume number row 2 was blocked on: 0.432718 m3 submerged at the cell-boundary plane,
0.452204 m3 at the nominal 0.30 m plane, total mesh 3.542739 m3.

Every number in that caption was recomputed and reproduces exactly. Row 2 stays UNVERIFIED on a
new, narrower blocker: **the divergence-theorem clipping routine that computed those volumes is
not in the repository.** The plotting script hardcodes the results and performs no geometry.

**One basis inconsistency, flagged not fixed.** Over-fill ratios (1.95 / 1.49 / 1.17) are taken
against the cell-boundary volume 0.432718, while the traction understatements (60 / 30 / 8.3
percent) are taken against the nominal-plane truth 3495.2 N, which derives from 0.452204. The
caption documents the choice, so it is a decision rather than an error, but it is mixed. On the
cell-boundary basis alone, true traction is 3600.3 N and understatements are 61.4 / 31.7 / 11.0
percent. Pick one before printing.

## BLOCKED

- Row 25, failure-mode decomposition: v1/v2 timeseries lack `vx,vy,vz`, classifier errors by
  design. Needs sweep regeneration.
- Row 31, `renders/mpm-engine-out/flood_vehicle/flood_vehicle.mp4` plus 45 frames: rests on a
  rendered MPM video, blocked by mission rule.
- Row 34, the coupled-MPM hero render with the real Yaris mesh: does not exist. Row 4 isolates
  that crash to `grid_density`.

No BLOCKED row gates the table. Rows 1, 4, 23, 24, 26, 35 and 36 ship without them.

## Stale-explanation files, flagged for a human, none edited

Still carrying the "n_grid=128 invalid because a surface-only ply solidifies hollow at fine
grid resolution" explanation, which seed 3 revises to a 60k sampling limit:

1. `docs/v3_invalidation_status.md`, self-titled "resolved (this file is current)"
2. `docs/track1_v3_sweep_invalid_hollow_vehicle.md`
3. `docs/COMPLETELY WRONG ON 3 COUNTS dont use track1_v3_sweep_invalid_hollow_vehicle.md`,
   byte-identical twin of 2
4. provenance-audit skill, Known-Error Register

Do not treat 1 as simply wrong. Its own "Honest nuance" section already concedes the shell
mechanism is present at all resolutions and names densifying the point cloud before solidifying
as the real fix, which is what seed 3 reports doing at 400k. It also argues about
`truck_trimmed.ply`, a genuinely sparse splat cloud, while seed 3 concerns the watertight Yaris
hull. Both can be true at once. Human call on wording.

Related: `docs/VERIFIED_FACTS_LEDGER_july24.md` A9 carries its own `[FLAG] D7: not reproduced on
this pass` and states the sampling-artifact hypothesis "is NOT excluded". Seed 3 answers that
open question. The ledger was modified by another pane during this pass and was not edited here.

## Three findings worth surfacing beyond the table

1. **`figures/baseline_comparison_v2.png` plots a synthetic curve.** `PEAK_DRIFT = 0.2884` is
   hardcoded and the trace is manufactured from an exponential plus three sines under
   `np.random.seed(7)`. No solver output is read. On a public NSF poster beside a caption
   implying a simulation trace, that is a false claim. Relabel as schematic or swap in
   `data/track1_sweep_v2/veh-sedan_dep-0p30_vel-1p50_idx-0004_timeseries.csv`.
2. **Only one asset has an intact script-plus-data chain, and it is stale.**
   `figures/phase_space_poster_figure.{png,svg}` predates its generator by 11 days and its input
   by 14. Everything else is an orphan, blocked, retracted, or hardcoded.
3. **The GitHub repo is private**, verified live. Beyond the QR code being dead, `README.md`
   embeds three images by absolute `raw.githubusercontent.com` URL at lines 32, 75 and 77; all
   three are broken for unauthenticated viewers.

## Contamination check, live

The addendum names `data/track2_sweep/manifest.csv` and `data/phase_space_results_mpm.csv` as
having received a null-result row. **Neither path exists on the Mac.** Only a root-level
`phase_space_results_mpm.csv` exists, mtime 07-14, eleven days before the null run. The
contamination is Vista-side and has not reached this working tree. No CSV was edited.

Note that `simulation/can_it_ford_L2_mpm.py:321` writes `data/phase_space_results_mpm.csv` while
`simulation/can_it_ford_L2_mpm_ytest.py:145` writes the root-level file of nearly the same name.
Do not assume they are the same file when reconciling.

## Unresolved, named not guessed

1. `warpmpm/vehicle.py` absent from this tree, so the 60,000-point resample is unconfirmed at
   the file. Vista only, mid-edit.
2. Seed 3's 400k numbers (+13.2 percent at 128, +56 percent at 192) have no artifact anywhere in
   this tree. Grepped repo-wide, zero hits.
3. The geometry routine behind 0.432718 / 0.452204 m3 is not in the repo.
4. Row 4 rests on a Vista-side document; all its arithmetic was independently recomputed here
   and confirmed, including the 0.567 percent free-fall discrepancy.
5. QR codes could not be decoded, no decoder on this machine. Needs a phone scan.
6. `flood_metrics.png` reading `metrics.csv` is inference from co-location, not verified.
7. `figures/poster_exports/can_it_ford_phase_space.*` provenance cannot be established; the only
   export script is dead on a missing `plot_phase_space` import.
