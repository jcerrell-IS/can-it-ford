# D2 — THE TWO LIVE EXPOSURES
worktree `.claude/worktrees/r5-exposure` · branch `claude/r5-exposure`
YOU OWN: `docs/E8_*`, `docs/CREDENTIAL_*`. Write nowhere else.
DIAGNOSIS ONLY. Never print a credential value, never rotate, never delete an
export line, never push.

## WHY YOU EXIST
**Exposure 1, the hull.** Verified live against `origin/main`:
`vehicle_geometry_research/yaris_coarse_v1l_watertight.ply`, blob `46a9f730`,
**12,445,769 bytes, on the PUBLIC remote**, plus three more derived .ply and 15
rendered .mp4/.gif. Register E8 forbids publishing derived NCAC/CCSA geometry
without written permission, and records the Yaris's licence side as UNRESOLVED.
A whole session spent last night enforcing an E8 hold on new frames whose source
hull is already public. Nobody owns this. NHTSA-hosted copies carry a
distribution statement; CCSA and GMU grant nothing; DOI 10.13021/G8JS5D has an
EMPTY rights field. GitHub has served removed blobs by SHA after a history
rewrite, so deletion does not unpublish.

**Exposure 2, credentials.** 12 to 15 across Vista, LS6 and the Mac. Sweep
complete (89/89 Mac roots). **ZERO rotated.** Diagnosis is finished; execution
has no owner. One prior session leaked a token into its own transcript while
investigating, so the transcript is itself a credential-bearing location.

## FIRST STEP
Establish provenance PER FILE for the four .ply. They may not share one answer.

## DEFINITION OF DONE
(a) A decision document: per file, origin, applicable licence, whether the
current public state violates E8, remediation options (leave / remove from HEAD
/ history rewrite / seek permission) with consequences, and a recommendation.
Do not execute it. (b) A numbered rotation checklist Josie can run top to
bottom: service, holding file, mode, cloud-synced?, in git history?, exact
rotation URL or command. No values. Ordered by blast radius.

## STANDING PROTOCOL (identical for all four, read once)
Before starting: read `/Users/josie/can-it-ford/.claude/tooling/ERRORS_AND_RESOLUTIONS.md`,
then `git log`, then `/Users/josie/can-it-ford/.claude/state/round5_board.md`.
Do not duplicate a sibling; append your own row to the board after each unit.

SELF-SUFFICIENCY. Decide for yourself. If a path, file, number or citation is
uncertain, GO FIND IT rather than asking: `corpus_resolve`/`corpus_search` for
any research file, `scite` or `scholar-sidekick` for any DOI, `wolfram` for any
unit or parameter, `deepwiki` for library behaviour (treat as hypothesis, verify
against source), `canford-tacc` for anything on Vista or LS6. If blocked, try a
genuinely DIFFERENT second approach, then write a named flag file and KEEP
WORKING on the rest of your scope. One blocker never ends a session.

CLAIM DISCIPLINE. Tag every claim: read directly / recalled / inferred. Report N
and spread, never a single draw. State the settle length behind any simulation
number. Run the physics-skeptic subagent before finalising any percentage,
force, verdict count or distance; if unavailable, say so and mark it UNREVIEWED.
An import succeeding is not an environment working. An empty result from one
directory is a broken probe, not an absence.

GIT. Commit each coherent unit as you finish it, path-limited:
`git commit -m "msg" -- <paths>`, 8 files max. Never bulk-stage. NEVER push. The
repo is PUBLIC. Writing to an absolute /Users/josie/can-it-ford/... path from
your worktree lands in the MAIN checkout: use paths relative to your own tree.
Never edit CLAUDE.md, the register, or sim_standing.py.

WHEN YOU FINISH A UNIT, keep going. Pick the next highest-value item in YOUR
scope. The auto-dispatcher will also nudge you, but do not wait for it.
No em-dashes.

