# R10 HANDOFF, 2026-08-20 12:15 BST

Written at Josie's instruction: *"write a handoff doc with all the content of your outputs and
my prompts"*. It carries her prompts verbatim, what each produced, and what is still open.

**Base `b0f1eda` → tip `1c1c64f`. 21 commits on `claude/add-ci-checks`, all pushed except the
last, plus `d34f24b` on `claude/r8-register` (local by design). 32 files created, 15 modified.**

Every number was measured live. Anything relayed says so. Anything uncheckable says that.

---

## 1. THE PROMPTS, AND WHAT EACH PRODUCED

| # | prompt, verbatim or abridged | outcome |
|---|---|---|
| 1 | The R9/R10 handoff pasted as the session brief | Chose the 13A audit lane. Found section 14 item 1 already resolved by `87ae518`, and two superseded numbers still in the file |
| 2 | *"look through all the sessions parented from this pasted session … what was found to be implemented globally and systematically on claude code to improve"* + `OK` / `BRUH` / `YIPPIE` | Audited `1d537aee` (239 agents) and `529261e9` (201). Hardened 18 hooks, found the connector bypass, built `verdict.py` |
| 3 | *"continu and what are next steps for me qand for yoou"* | Six items for Josie, four for me, with a recommendation |
| 4 | *"deploying daughtr sessions … start a workflow across many sessions"* | Probed the Agent path (alive), ran a **15-agent workflow**: 7 readers, 7 adversarial verifiers, 1 planner |
| 5 | *"try now and continue and also please use your best judgement and full disk use"* | Used the **browser** to read two MDPI papers I had wrongly assigned to Josie. Both died on the scope test |
| 6 | *"do now and audit yourself and be self-sufficient to use the research corpus readers …"* | PHASE 0 guards, PHASE 1 register lineage, the reproducibility manifest, the licence resolution |
| 7 | *"save everything absolutely everything form this session to memmory"* | 4 new memory files, 4 corrected, `R10_SESSION_RECORD_2026-08-20.md` |
| 8 | *(peer session sends four corrections)* | Verified all four independently. All four confirmed; two corrected me |
| 9 | *"sockets opened just do this now on vista and ls6 in a node you can only act within a node, and then write a handoff doc"* | Took nodes on both machines, measured both, corrected the manifest. **This document** |

---

## 2. THE PEER AUDIT, VERIFIED RATHER THAN ACCEPTED

A second session sent four corrections. This project's rule is that a claim from Claude is not
evidence, so each was re-derived. **All four hold. Two of them correct me.**

### 2.1 Vista was never blocked, and I told Josie it was. MY ERROR.

`scripts/tacc.sh vista 'hostname'` returned `login2.vista.tacc.utexas.edu` in **0.72 seconds**.
LS6 likewise. My claim that both needed her MFA rested on a `--status` call failing, which is
**not evidence the host is unreachable**. That is the same predicate-shaped false negative this
session spent all day recording, committed against my own user in a to-do list.

### 2.2 The licence fix has not reached the public. CONFIRMED.

`origin/main:CITATION.cff` still reads `license: ODC-By-1.0`. My "ahead by 0" was true of the
feature branch only. Re-measured after a fresh fetch: **`claude/add-ci-checks` is 123 ahead and
5 behind `origin/main`.** GitHub renders the citation widget from the **default branch**, so the
CC-BY-4.0 change is invisible to anyone reading the repo.

### 2.3 `hf_space/` on this branch is a fossil that would REGRESS the live Space. CONFIRMED.

- `hf_space/README.md:27` says **"Genesis MPM"**, violating CLAUDE.md item 1
- `hf_space/app.py:4` says `HAZARD_THRESHOLD_M2S = 0.60`, the Large 4WD figure applied to a Yaris
- **This branch has touched `hf_space/` exactly 0 times since merge-base `1a868f3`**
- `origin/main`'s version implements the **joint rule** and carries no bare 0.60

**Never run `hf upload josiecerrell/can-it-ford ./hf_space .` from this branch.** Edit against
`origin/main`'s copy or the Space head. There is no *automatic* regression path, because the CI
sync fires only on push to `main` with a `hf_space/**` paths filter; the hazard is a manual
upload only.

### 2.4 I audited the wrong `sweep-v1`. MY ERROR, and it is the same shape as the register A2 bug.

**There are TWO public repos with that name.** I found the dataset (30 downloads, empty, with an
honest warning README) and reported it. The **MODEL** repo
`models/josiecerrell/can-it-ford-sweep-v1` is also public, its only tag is `region:us`, so it
carries **no licence, no README and no cardData**, and per the peer it holds 37 real files of
box-proxy-lineage timeseries with `density_plausible=False` on all 36 rows.

**That is the actually-unlicensed public artifact carrying real superseded data**, and I missed
it by assuming one name meant one repo. I made the identical mistake twice in one day: register
`A2` also meant two different things.

---

## 3. WHAT THE NODES MEASURED

Both taken at **10-minute** walltime, because the recorded lesson is that oversized requests are
what block backfill.

| | Vista | LS6 |
|---|---|---|
| job / node | 924231 / `c608-062` | 3378048 / `c301-003` |
| partition | `gh` | `gpu-a100-dev` |
| arch | **aarch64** | **x86_64** |
| CPU | Neoverse-V2, 72 cores | AMD EPYC 7763 64-Core |
| GPU | **NVIDIA GH200 120GB**, 97871 MiB | **3× NVIDIA A100-PCIE-40GB**, 40960 MiB |
| driver / cc | 590.48.01 / **9.0** | 570.195.03 / **8.0** |

**The Vista figure is two independent origins**: the peer measured identical hardware on
`c611-021` under job 924230.

**The LS6 measurement closes a caveat I wrote this morning.** I had corrected "LS6 is aarch64" to
x86_64 by *inference* from a build directory named `chrono_x86_build`, and flagged it as needing
a live `uname -m`. It now has one. The two machines differ in **architecture AND GPU
generation**, so no build, wheel or container transfers between them without checking.

**Allocation: 591 SU, expiring 2026-09-30.** Both jobs cost single-digit SU.

---

## 4. THE WALL-TIME ROW WAS WRONG, AND IT UNDERSTATED THE PROBLEM

My manifest said wall-time-per-simulated-second *"lives in Slurm accounting on Vista"*, which
implies go and fetch it. **It cannot be fetched.**

- Slurm accounting **does** still hold **212 job records** for 2026-07-01 to 2026-08-10
- **No `summary.json` carries a job id, host or timestamp.** Verified across all 17: the only key
  matching a job/host/timing pattern is `C2_veh_zmin_start`, a **geometry** field caught on the
  substring "start"
- mtimes do not help: `summary.json` was regenerated 2026-08-12 while `rollout.npz` dates from
  2026-07-26

**A missing remote value and an unjoinable one are different findings.** It is recoverable only
for FUTURE runs, by emitting `SLURM_JOB_ID` into `summary.json`. The manifest now says exactly
that.

**And my first attempt at that check read 3 of 17 summaries**, because I built paths directly
instead of using the recorded `summary_path`. The manifest was always right; my ad-hoc check was
not, and it would have reported a smaller denominator.

**The manifest now reads: 6 of 6 recoverable items PRESENT, 1 ABSENT with the honest reason,
1 N/A.**

---

## 5. THE SESSION'S OTHER STANDING RESULTS

Full detail in `docs/R10_SESSION_RECORD_2026-08-20.md`. In one line each:

- **The adversarial panel judged 20 claims, not 7.** 17 refuted, 4 recorded. Three of the
  thirteen unrecorded refutations are claims the handoff still carries forward.
- **The corpus index saw 8 of 21 deep searches**, now 21 and tracked. `--query` was author-blind.
- **Six literature candidates dead on the scope test, five on one ground**: every SPH boundary
  mechanism needs boundary particles, and this floor writes only a grid-node velocity.
- **The reproducibility record is the contribution** — 17/17 runs, 10/10 fields, zero failures.
  Josie decided to claim it in the paper.
- **Register `A2` meant two things 2,100 lines apart**, one being "Gravity is -9.81 and was never
  unknown". All three colliding ids now `R9D-` prefixed.
- **`disableClaudeAiConnectors: true` is set and does not take effect.**
- **The adversarial review path is alive again** after being recorded dead across nine origins.

---

## 6. EVERY ERROR OF MINE, TWELVE NOW

| # | what | caught by |
|---|---|---|
| 1 | `cm_floor_check` failed 23 of 23 | uniform result distrusted; three estimator bugs |
| 2 | Shell DOI scan false zero on a file with 34 DOIs | re-run in python |
| 3 | `git add $batch` passed 22 paths as one argument | zsh word-splitting, already in memory |
| 4 | Assigned two MDPI PDFs to Josie as a human task | I had browser tools |
| 5 | Shipped a false pass in the tool built to stop false passes | **the workflow's adversarial verifier** |
| 6 | Exit-code test piped to `head`, every branch read as 0 | re-ran without the pipe |
| 7 | Wrote that the register id collision did not reproduce | wrong grep pattern |
| 8 | Wrote that both public Spaces were unlicensed | Hub tag listing read as front matter |
| 9 | `grep -c \|\| echo 0` producing `0\n0` in my own scan | instrument failure #2, self-inflicted |
| 10 | Two `PARTIAL` rows that were my own key-naming bug | **understated** the project |
| 11 | **Told Josie Vista needed her MFA when it answered in 0.72s** | **the peer session** |
| 12 | **Audited the dataset `sweep-v1` and missed the model of the same name** | **the peer session** |

**Ten of twelve were caught inside this session; two needed a second pair of eyes.** Errors 7,
11 and 12 are all one failure: **a partial view treated as the whole**, three times in one day.

---

## 7. OPEN, IN PRIORITY ORDER

1. **`models/josiecerrell/can-it-ford-sweep-v1` is public, unlicensed, with no README and real
   superseded data.** Highest-value single fix. Needs Josie's call: licence, privatise, or delete.
2. **The CC-BY-4.0 change has not reached `origin/main`**, which is what GitHub renders. Requires
   merging `claude/add-ci-checks` (123 ahead, 5 behind) or a targeted PR.
3. **PHASE 2–3 of the workflow plan**: 14 register rows, 12 CLAUDE.md corrections.
4. **~115 of 135 deep-research claims and ~350 of 399 R10 findings unrouted.** Commands in
   `R10_JOURNAL_AUDIT_2026-08-20.md` section 8.
5. **Emit `SLURM_JOB_ID` into `summary.json`** so wall-time becomes joinable for future runs.
6. **The Azhar flotation ladder** is now genuinely unblocked; both machines answer.
7. **The CI `continue-on-error` mask can come off**, now that NOT-EVALUABLE exists.
8. **`MEMORY.md` is 28.5KB against a 24.4KB limit**, 87 of 90 entries over ~200 chars. An
   over-limit index is truncated on load. **I did not trim it**: `.claude/memory/` is gitignored
   so there is no history to recover from, and it means editing 87 entries other sessions wrote.
   Needs an explicit go.

## 8. HARD RULES LEARNED TODAY

- **Never upload `hf_space/` from this branch.** It is a fossil predating PR #11.
- **A `--status` failure is not evidence a host is unreachable.** Try the direct command.
- **One name can mean two repos, and one id can mean two register items.** Check for a second.
- **The Hub tag listing is a partial view of front matter.** Read the README.
- **A publisher 403 is evidence about the host, never about the literature.** Try the browser.

---

## 9. ADDENDUM 12:25, SECOND-EYES ROUND 2, ALL VERIFIED INDEPENDENTLY

### 9.1 W&B CORROBORATES THE UNJOINABLE VERDICT, AND CARRIES A TRAP

W&B records runtime and GPU automatically, so it is the obvious recovery route for
wall-time. **It is not one, and how it fails is worse than being empty.**

Measured by read-only GraphQL against project `can-it-ford`, 106 runs:

| field | value, on all 17 canonical runs |
|---|---|
| `host` | `Josephines-MacBook-Air.local` |
| `runInfo.gpu` | `None` |
| `runInfo.os` | `macOS-26.5.2-arm64` |
| `_runtime` | **0** |
| created | 2026-08-17T21:30-21:31, **3 to 4 seconds apart**, heartbeat 1s later |

They are a **backfill from the laptop**, not the GPU runs.

> **THE TRAP: the W&B dashboard shows a Runtime column reading 0 seconds for seventeen GPU
> simulations.** A plausible-looking number that means nothing. Never read timing for the
> canonical runs off the W&B UI.

That is the same shape as every instrument failure this project records: a value
indistinguishable from a measurement when nothing was measured. It is now recorded in
`analysis/reproducibility_manifest.py` beside the unjoinable reason, which is where anyone
chasing wall-time will actually look.

**It also counts as a second origin under this project's own rule**: a different system, a
different record, a different failure mode. Slurm has the timing and no join key; W&B has the
join and no timing.

**And it closes the other shortcut.** W&B's config carries `driver_sha256`, `engine`,
`gravity_ms2`, `vehicle_asset`, `solid_volume_m3` and an honest `settle_frames_source` that the
manifest lacks, but it does **not** carry `canitford_git_commit`, `solver_git_sha` or
`mesh_sha256`, so it cannot recover those either. [relayed from the peer, not re-derived]

### 9.2 I LICENSED A SPACE THAT HAS NO APP

`spaces/josiecerrell/can-it-ford-demo` is public with runtime stage `NO_APP_FILE`. Verified by
downloading it: the whole Space is **three files**, `.gitattributes`, `README.md` and
`phase_space_results.csv`. Its README declares `app_file: app.py` **and no `app.py` exists.**
Broken publicly since 2026-08-18.

**I added a licence line to it at 11:52 today.** The licence is correct and independent of the
app, so nothing was made worse, but the honest statement is that the Space is now **correctly
licensed and still broken**. Fixing it means shipping an `app.py` or removing the `app_file`
declaration, and either is a public write that needs a go.

### 9.3 THE DEFAULT BRANCH HAS NO CI AT ALL

`.github/workflows/canford-checks.yml` is **absent from `origin/main`**, confirmed with
`git cat-file -e`. So the default branch gets **no `params_check`, no physics gates, no
stationarity self-test**. And on this branch, `continue-on-error: true` is set on two of six
steps, `register_integrity` (line 22) and `count_claims` (line 25), so neither can turn a run
red.

**The `count_claims` mask can now come off**, because `85c247f` gave that check a
NOT-EVALUABLE branch; before that, removing the mask would have failed every worktree-rooted
run on phantom defects. The `register_integrity` mask has not been examined and should not be
removed on the strength of this note.

Six open GitHub issues, all from 2026-07-09/10; per the peer, #7, #6 and #2 are stale-resolved
and #3 duplicates #5. [relayed, not re-derived]
