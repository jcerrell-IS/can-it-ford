# Figure and data catalog, by poster/paper section

Built 2026-07-26. Every entry has a real file path and, where one exists, a real generating
script. Ranked within each section by how well it shows PROCESS, not by how well it answers
the original research question. Stale or invalidated items are included on purpose, labeled
as evidence of catching your own error, not hidden.

---

## Video and GIF provenance, resolved completely

You uploaded 16 file paths. They reduce to 8 distinct files (verified by md5 hash just now).
Two names that looked like different renders turned out to be the same content at two export
resolutions. Full chain for all eight, earliest to latest:

| asset (by hash) | true source path | what it is | resolution |
|---|---|---|---|
| `flood_vehicle_yaris.mp4`, 272,270 B | `renders/yaris_L2_d0p30_v1p5/flood_vehicle_yaris.mp4` | **Job 866214, the first render-verified real-mass Yaris MPM run.** 880x616, 45 frames, 6.43s. This is THE breakthrough video. | 880x616 |
| `yaris_flood.mp4`, 4,962,780 B | `figures/yaris_flood.mp4` | Dry-start scenario, later superseded by the standing-water fix. Same content as the SUPERSEDED gif. | full |
| `yaris_flood_standing.mp4` = `FIG3_animation_standing_water.mp4` | `figures/yaris_flood_standing.mp4` | First correct-scenario (standing water) render | full |
| `hero_g64_m1100.mp4` (bare name) | `renders/yaris_render_s1/hero_g64_m1100.mp4` | Early hero attempt, shadows still on, water renders opaque | — |
| `hero_g64_m1100_FIXED.mp4` = `..._WATER.mp4` | `renders/yaris_render_s1/hero_g64_m1100_FIXED.mp4` | **The poster's actual Fig 3 hero.** Shadow pass removed, water translucent, `open_edges=0`, `+1.68%` volume error | — |
| `yaris_g64_m1100_STANDING_dx0p659_top.mp4` | `renders/yaris_render_s1/yaris_g64_m1100_STANDING_dx0p659_top.mp4` | Top-down diagnostic, parameters printed on-frame | — |
| `yaris_g64_m1100_STANDING_dx0p659_oblique.mp4` (no date prefix) | same run, 1364x832 export | Oblique diagnostic, higher-res export | 1364x832 |
| `2026-07-26__..._oblique.mp4` (date-prefixed) | same run, 1200x720 export | **Identical 90 frames, identical 3.75s, same scenario.** Just a smaller export pass of the file above. Not a second render. | 1200x720 |

**How every one of these was made, no exceptions:** `sim_standing.py` writes `rollout.npz`
(particle positions per frame) → a render script (`render_flood.py`, matplotlib, two-panel
diagnostic; or `render_pv.py`, PyVista, the photoreal hero) writes one PNG per frame →
`encode.py` calls `imageio_ffmpeg` for the mp4 and PIL for the gif. Nothing here was
screen-recorded or hand-made.

---

## Section 1: "What I set out to build" / Introduction

No figures belong here. This is the one section that should stay text-only, since its whole
job is stating intent before anything happened to it. If you want one visual: the empty
5-stage pipeline (no annotations), as a clean contrast to the annotated version in Section 2.

---

## Section 2: The pipeline, annotated with what happened

**Strongest asset: the pipeline diagram itself**, built fresh in the poster you already have.
No pre-existing script generates this exact annotated version; it's original content, written
from the git log and `.claude/handoffs/`. Caption it: *"Six intended stages. Gray text worked
as planned. Amber is a real bug, found and fixed. Red never closed. Built from commit history
and session handoffs, not from a template."*

**Supporting data, one per stage, all traceable:**
- Stage 3 (particle seeding): `.claude/handoffs/2026-07-25_MPM-REAL-yaris-verified.md`, the
  fill-ratio table across 4 grid sizes, before/after `solidify_watertight`. This is your best
  single piece of "I found the bug, here's the proof, here's the fix" evidence in the whole
  project. **Use it as a real figure**, not just prose: 4 rows, ratio and rho, pre- and
  post-fix.
- Stage 4 (fluid solver): no chart needed, this is a one-line fact (SPH claimed as MPM), best
  as the two-row table already in your poster.
- Stage 6 (verdict): `data/scenario_sweep.csv`, the 37→14 FORD-count bar. You already have
  this, script `scripts/gen_scenario_sweep.py`.

---

## Section 3: Methods

**Strongest:** the four-rung ladder description, text only, sourced from `vehicle_params.py`
`AR_R_STABILITY_LIMITS` directly. Don't chart this, it's a definition, not a result.

**If you want a Methods figure:** `figures/pipeline_diagram_poster.svg`, generated by
`analysis/plot_geometry_pipeline.py`. Caption: *"The reconstruct-to-decide pipeline as
implemented. Solid arrows are stages exercised this session; dashed arrows are stages the
dispatch code supports but that have not yet run on real reconstructed geometry."* Be
explicit in the caption about which arrows are real and which are aspirational, since this is
exactly the kind of figure that overclaims by default.

---

## Section 4: Figures and Results, the load-bearing ones

Ranked by how well-verified each is, strongest first.

1. **Velocity sweep** (`figures/g1_velocity_sweep.png`, `analysis/fig4_velocity_regime.py` or
   `analysis/make_poster_figures.py`). Six points, monotonic, already in your poster.
   Caption: *"Final displacement vs surge velocity, 1100 kg, grid 64, from
   `data/all_runs_inventory.csv`. Two points fall below the AR&R FORD line."*

2. **Mass sweep** (`figures/g5_mass_sensitivity.png`,
   `analysis/fig2_mass_sensitivity.py`). D x V bit-identical, displacement not. Your
   strongest "the rule can't see this" figure.

3. **Traction bias** (`figures/traction_bias.pdf`, `analysis/plot_traction_bias.py`, full
   caption already written at `figures/traction_bias_CAPTION.md`). **The most rigorous
   single figure you have.** Ten explicitly stated assumptions, a full values table, a
   one-sided-bias argument (not just an error bar). This deserves more real estate than it
   currently gets. Caption exactly as written in the CAPTION.md file, don't shorten it, the
   assumption list is the credibility.

4. **Verdict matrix** (`figures/g3_verdict_matrix.png`). Confirm which script wrote it
   (`analysis/plot_l1_three_class.py` is the most likely candidate per the tripwire values in
   that script) before captioning it as verified; if you can't confirm the script, caption it
   *"regenerated by hand from `data/scenario_sweep.csv`, script not confirmed"* rather than
   implying full automation.

5. **Bow-wave / local depth** (`figures/g4_bow_wave.png`). Supports the "measures the wrong
   water" point. Caption should state the 19-of-20 figure precisely, not "every run."

---

## Section 5: Where I was wrong, presented as evidence, not apology

This is where stale and invalidated material belongs, explicitly labeled as such. Do not
delete or hide any of these, presenting them accurately IS the content.

1. **`figures/yaris_flood.gif` / `.mp4`, the dry-start scenario.** Caption: *"Superseded.
   This scenario applied surge velocity once instead of continuously, understating
   displacement by 7.13x compared to the corrected version. Kept here as the before, not the
   result."*

2. **`data/track1_sweep_v2/manifest.csv`.** Built by anisotropically warping
   `truck_trimmed.ply` to a bounding box. A same-condition unwarped run diverged 4.6x
   (0.7702 m vs 0.1662 m). Caption: *"Excluded from every result on this poster. Shown here
   only to demonstrate the check that caught it: running the same scenario both ways."* This
   is your best "I verified my own work and it failed" figure. Genuinely worth a panel.

3. **The old poster, `figures/Cerrell_TACC_42x56.pdf` (2026-07-25, 17:13).** No generating
   script found anywhere in scope (`OPEN_ITEMS.md` O-14). If you show this at all, caption it
   *"an earlier draft, superseded, kept for the record, its generating script could not be
   located, which is itself the lesson: it's not auditable, so it's not used."*

4. **The two agreeing-but-wrong verification scripts**, `verify_poster_numbers.py` and the
   original `scenario_sweep.csv` before the float fix. No chart needed, a two-column table:
   what each one said, why both were wrong, and that they didn't even agree on which cells.

---

## Section 6: Where I could go from here, and what it took to get exposed to it

Every item below is real code already on disk, not aspirational.

- **`analysis/gp_surrogate.py`.** A Gaussian process classifier and regressor
  (scikit-learn). First real exposure to surrogate modeling for expensive simulations.
  Currently trained on invalidated data (Section 5, item 2), so its accuracy numbers mean
  nothing yet, but the feature set and under-resolution exclusion logic are sound. **Could
  cumulatively answer the original question faster than re-running MPM per query**, once
  pointed at valid geometry.
- **`analysis/viability_dashboard_scaffold.py`.** Full 6-DOF failure-mode infrastructure
  (slide/topple/float as separate classes, not just FORD/NO-FORD). Needs real run data
  wired in, the design work is done.
- **PyVista, first real exposure**, via `render_pv.py`, for the photoreal hero renders.
- **Native Genesis MPM API**, `MPM.Liquid`, `coup_friction`, first hands-on contact via the
  Track 1 rebuild, still separately blocked on Track 2 (`can_it_ford_L2_mpm.py`,
  `CUDA_ERROR_ILLEGAL_ADDRESS`).
- **NCAC/CCSA crash-test FE meshes** as a vehicle-geometry source, first exposure via the
  Yaris hull search after two DIY reconstruction attempts failed.

---

## Section 7: Conclusion

No new figures. If one is wanted, the single best closer is the `flood_vehicle_yaris.mp4`
breakthrough video (top of this catalog), captioned with its own numbers: *"Job 866214,
2010 Toyota Yaris, 1100 kg, correct displaced volume for the first time, final |d| = 0.09043 m.
Supersedes every prior render for every purpose."* It's the one video on your whole desktop
that is unambiguously "this is when it started working," and it hasn't been used as a closer
anywhere yet.
