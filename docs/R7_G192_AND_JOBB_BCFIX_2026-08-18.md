# ROUND 7, TASKS 2 AND 3: g192 COLLECTED, AND THE BOUNDARY-FIX A/B GRADED

**Date 2026-08-18. Branch `claude/r7-collect`.** Both jobs were already paid for and sitting
uncollected at the end of Round 6 (handoff section 11b, rows 2 and 3). No new GPU was
submitted for either.

Every number below is regenerated by a script in this commit, with **one stated exception**:
limb 3 of the Job B prediction is graded by hand, not by `verdict_on_prediction()`. Nothing is
transcribed from a prior summary:

- `analysis/r7_ladder_stats.py` regenerates the whole g48 to g192 ladder table.
- `analysis/r7_jobb_bcfix_ab.py` regenerates the entire Job B A/B.

```
/opt/homebrew/bin/uv run --with numpy python3 analysis/r7_ladder_stats.py --base <dir>
/opt/homebrew/bin/uv run --with numpy python3 analysis/r7_jobb_bcfix_ab.py \
    --control <918240.json> --treatment <918450.json>
```

---

## PART 1. JOB 918351, g192, FIVE REPEATS

`COMPLETED 00:10:50, ExitCode 0:0`, verified live by `sacct -X -j 918351`. Output at
`$WORK/r6_rep_g192_918351/rep_1..5/`.

### The answer

**g192 is STUCK 5 of 5. The g160 flip is not a one-grid artifact: it holds at the next
refinement level too.**

```
job 918351   g192_m2337   5 repeats   90 frames   canonical driver
verdicts     STUCK x5     joint frames [0,0,0,0,0]     margin [-3,-3,-3,-3,-3]
dx 0.049072 m   water_layers 12   n_water 1594532   all 5 metrics.csv bit-distinct
```

Every item asked for, in order:

| asked | measured |
|---|---|
| verdicts | **STUCK 5/5** |
| joint frames | **[0, 0, 0, 0, 0]** |
| margin | **[-3, -3, -3, -3, -3]** |
| all five `metrics.csv` bit-distinct | **YES**, 5 files, 5 distinct sha256 |
| `water_layers` | **12** |

The five sha256 digests, so the distinctness claim can be re-checked rather than trusted:

```
rep 1  7935dfa7fc60edd98480082f6535cc9d05aeb1b82f51f55e981b27c81fc4046b
rep 2  725d095964f65708a53dc30e62f5797e7012c75d9356214f3e8ac8c665a4b4bb
rep 3  f50bf35aed513903d59f2346627efac547c9021a33540aa6a531a69322a92ac1
rep 4  34744d0cb419d128b3d163c989e43a43197210ce920a93f1f1dab3d8d1e45956
rep 5  1696e512301498fcb3d98a308e53b7f00a812ca306147e57c85a312d89bef542
```

### `water_layers` is 12 at g192, NOT 10

The task framing said g160 "was the first grid to reach 10 layers and that is where the flip
landed". The first half is right and the second needs one qualification: **g192 reaches 12
layers, not 10, and it is also STUCK.** So the flip landed at the first grid meeting the
literature convention AND it persists once the convention is exceeded. That is the stronger
of the two readings and it is the one the data supports.

`water_layers` straight from each `summary.json`:

| grid | 48 | 64 | 96 | 128 | 160 | **192** |
|---|---|---|---|---|---|---|
| water_layers | 3 | 4 | 6 | 8 | 10 | **12** |

### The extended margin table

Recomputed from the raw `metrics.csv` of all 30 runs with the published classifier
(`simulation/failure_modes.py`, `ssf = 1.42`, `G = 9.81`, mass 2337 kg, window rows 0 to 90),
not copied from the previous table:

| grid | dx (m) | water layers | verdict (N=5) | joint frames | margin | span (m) |
|---|---|---|---|---|---|---|
| g48 | 0.19629 | 3 | SLIDE 5/5 | 11,11,11,11,11 | 8 | 7.8515 |
| g64 | 0.14721 | 4 | SLIDE 5/5 | 9,9,9,**10**,9 | 6 to 7 | 8.2440 |
| g96 | 0.09814 | 6 | SLIDE 5/5 | 3,**4**,3,**4**,3 | 0 to 1 | 8.6366 |
| g128 | 0.07361 | 8 | SLIDE 5/5 | 3,3,3,3,3 | 0 | 8.8329 |
| **g160** | 0.05889 | 10 | **STUCK 5/5** | 0,0,0,0,0 | **-3** | 8.9507 |
| **g192** | 0.04907 | **12** | **STUCK 5/5** | 0,0,0,0,0 | **-3** | 9.0292 |

**The g48 to g160 verdicts and joint-frame lists reproduce the published record**, with one
margin presented differently, so do not read "exactly". Note: the
Round 7 handoff's compressed table gives g64 margin as `6`, which is the modal value; the full
list is `6,6,6,7,6` and rep 4 sits at 7. `docs/R6_A2_REPEATS_AND_JOBB_MEASURED_2026-08-18.md`
line 193 already carries the full list correctly, so this is a compression artifact in the
summary table only, not an error in the underlying record. Quote the range, `6 to 7`, the way
g96 is quoted as `0 to 1`.

### The `sustain_frames` fragility is gone at both g160 and g192

`sustain_frames = 3` at `failure_modes.py:52` is unsourced and, at g128, a one-integer change
flips all six draws. That fragility **cannot** apply at g160 or g192: the joint slide condition
holds for **zero** frames in all ten draws across the two grids, so the verdict is STUCK for
any `sustain_frames >= 1`. Tested further after review: the joint run stays 0 at both grids
down to `slide_m = 0.040` and `slide_speed_ms = 0.010`, so it is robust to the other two
unsourced literals as well.

**BUT THE MECHANISM IS NOT WHAT AN EARLIER VERSION OF THIS DOCUMENT SAID, AND THE CORRECTION
MATTERS.** That version wrote "under-resolved gives a fragile SLIDE that an arbitrary integer
can flip; resolved gives a STUCK that no choice of that integer can move, a coherent picture".
**That reads as a physical mechanism and it is not one.** Measured live on rep_1 of each grid,
window 0 to 90:

| grid | max surge drift | frames with drift >= 0.05 m | frames with speed >= 0.05 m/s | joint |
|---|---|---|---|---|
| g48 | 0.18081 | 4..90 (87) | 1..14 (14) | 11 |
| g64 | 0.13917 | 4..90 (87) | 1..12 (12) | 9 |
| g96 | 0.08611 | 5..90 (86) | 1..7 (7) | 3 |
| g128 | 0.07497 | 6..90 (85) | 1..8 (8) | 3 |
| **g160** | **0.05497** | **29..87 (59)** | **1..6 (6)** | **0** |
| g192 | 0.04757 | **none** | 1..6 (6) | 0 |

**The speed criterion is satisfied only in the first 6 to 14 frames at EVERY grid**, because
`sim_standing.py:240` applies a one-shot `v[:n_water,0] += velocity` kick to all water at the
end of settle, and that kick then decays. So the joint condition is decided by whether the kick
carries the hull past 5 cm of drift **before the kick dies**, not by whether the flow sustains a
slide.

**g160 and g192 are STUCK for DIFFERENT reasons, and only one of them is physical.** g192 never
crosses the 5 cm drift threshold at all (max 0.04757 m), which is a real result. **g160 DOES
cross it, for 59 frames, reaching 0.05497 m**, and is classified STUCK purely because drift does
not begin until frame 29 while speed expired at frame 6. Calling g160 "STUCK" when the hull
drifted 5.5 cm is a **classifier naming artifact of the joint condition**, not a finding that
the vehicle held station. Do not present the g160 row as physical stasis.

### THE CONFOUND IS NOT RESOLVED AND g192 INHERITS IT, AND MAKES IT WORSE

`grid_lim` is **identical at 9.42174 m on all six grids**, which is exactly why this trap is
easy to fall into. But the driver sets `wall = 4*dx` on each side, so the usable water span is
`grid_lim * (1 - 8/n_grid)` and dx shrinks with n:

| grid | span (m) | vs g48 |
|---|---|---|
| g48 | 7.8515 | 1.0000x |
| g64 | 8.2440 | 1.0500x |
| g96 | 8.6366 | 1.1000x |
| g128 | 8.8329 | 1.1250x |
| g160 | 8.9507 | 1.1400x |
| **g192** | **9.0292** | **1.1500x** |

**The tank is LARGEST exactly where the verdict is STUCK.** Refinement and tank enlargement are
co-directional across the entire ladder, and adding g192 extends the confounded range rather
than breaking it: g192 is both the finest grid and the biggest tank. This run cannot separate
the two, and neither can any other run in the ladder.

**Report this as "the verdict flips under a refinement that also enlarges the tank", never as
"the verdict flips under refinement".** The pinned-span control
(`scripts/pinned_span_wrapper.py`, handoff item 1) remains the highest-priority experiment in
the project, and g192 raises its value rather than reducing it: there are now two STUCK grids
to explain, not one.

### THE STRONGEST RESULT IN PART 1 IS CONTINUOUS, NOT BINARY, AND IT IS NOT CONVERGED

Added after review, because the binary verdict buried it. Max surge drift over rows 0 to 90,
N=5 per grid, mean +/- sd:

| grid | max surge drift (m) | sd | cv | change vs previous |
|---|---|---|---|---|
| g48 | 0.18119 | 0.00024 | 0.13 % | |
| g64 | 0.13825 | 0.00073 | 0.53 % | -23.7 % |
| g96 | 0.08597 | 0.00022 | 0.26 % | -37.8 % |
| g128 | 0.07399 | 0.00100 | 1.36 % | -13.9 % |
| g160 | 0.05643 | 0.00093 | 1.65 % | -23.7 % |
| g192 | 0.04732 | 0.00052 | 1.10 % | **-16.1 %** |

sd is the POPULATION sd over the 5 repeats, stated because a sample sd gives slightly larger
figures and the two must not be mixed. Means and percent changes were recomputed independently
and agree to all digits shown.

**This is cleanly monotone with tiny repeat scatter, unlike the displacement magnitudes in
CLAUDE.md item 5.** It is also **not converged**: drift is still falling 16.1 percent per
refinement level at g192 with no asymptote in sight. Per CLAUDE.md item 5 the VERDICT is what
may be cited and the magnitude is not, which this document observes; but a reader is entitled to
know the underlying quantity has not settled, because that bears directly on whether the g160
flip means anything.

### The confound is understated by the linear ratio

The span table above reports the LINEAR ratio. The walls move in x and y together, so the
quantity that matters for water mass and fetch scales as the square:

| grid | span/g48 | area/g48 | water volume/g48 |
|---|---|---|---|
| g48 | 1.000 | 1.000 | 1.000 |
| g192 | 1.150 | **1.323** | **1.369** |

**The tank holds about 37 percent more water at g192 than at g48, not 15 percent.** Quote the
volume ratio, or say explicitly that 1.15x is linear.

**A second co-varying geometry, omitted entirely from the earlier version.** `sim_standing.py:223`
sets `_inflow_x = wall + 1.5` while the vehicle sits at a constant `0.60*lim = 5.6530` m, so the
gap from the clamped inflow slab to the hull grows monotonically from 3.3679 m at g48 to 3.9568 m
at g192, **+17.5 percent**. That is more travel distance for the forcing, directly on the causal
path to surge drift, and it is confounded with resolution exactly as the tank size is.

**One thing that is genuinely held fixed, and is worth stating because it forecloses the obvious
alternative:** realized water depth is **0.2944294 m on all six grids**, identical.

### What g192 does and does not settle

**Does:** the g160 flip is not a single-grid fluke, it is not repeat noise (5 of 5 at each of
two grids, all ten files bit-distinct), and it is not sensitive to the unsourced
`sustain_frames` threshold.

**Does not:** it does not establish that resolution is the cause, because of the confound
above. It also remains one mass, one depth, one velocity: m2337 at 0.30 m and 1.5 m/s. The
published 16 SLIDE / 1 STUCK headline covers 17 configurations and only this one has been
carried up the ladder.

---

## PART 2. JOB 918450, THE BOUNDARY-FIX A/B

`COMPLETED 00:06:53, ExitCode 0:0`, `RC_Bbc=0`, `ALLDONE`, verified live by `sacct -X -j
918450`. Output at `$WORK/d4_jobBbc_918450/sphere_fixed_bcfix.json`, 200 frames.

### The treatment is one line, and that was verified rather than trusted

`diff -rq -x __pycache__` over `$WORK/mpm-engine/src` against `$WORK/mpm-engine-bcfix-src`,
run live 2026-08-18: **exactly one source file differs**, and its diff is exactly one line.

```
warpmpm/kernels/mpm_solver_warp.py
1955c1955
<                 if dotproduct < 0.0:
---
>                 if dotproduct <= 0.0:
```

Everything else that `diff -rq` reports is `__pycache__/*.pyc`. The pinned engine is untouched,
so all provenance holds.

```
control engine   285139395097a914b883fe114e8633cf8c8cf8ccc3a3afbbb57c7e3aa1f12128
bcfix   engine   2309d8a2358d693e91d54e97c80483a5228827d3bb694a326ac7fae1d9cae35a
scene (bcfix)    6ab8cec5fa2acac226954d9a5218fa3de841da1f7ef0a175789a1d4212396a84
```

**ONE CAVEAT ON "IDENTICAL SCENE", found by comparing the emitted config blocks.** Of 78 config
keys, **72 are identical and 6 are absent from the control**: `band_m`, `band_mult`,
`ghost_depth_m`, `n_ghost_layers`, `sdf_band_exceeds_bspline_halfwidth`, `sdf_band_over_dx`.
So the treatment ran a **newer revision of `sphere_heave.py`** than the control, not the same
file. Those six are new reporting fields at their defaults, and both defaults are documented to
reproduce the earlier path exactly: `band_mult = 1.0` "reproduces the engine default exactly"
(`sphere_heave.py:602`) and `n_ghost_layers = 0` restores the original seeding-floor and `n_z`
expressions (`6ed163e`). So the **effective configuration is identical and the A/B stands**, but
the run script's "identical in EVERY respect except the engine" is true of the configuration,
not of the scene file revision. The control's job log predates the sha256 echo lines, so its
as-run scene file cannot be hash-compared; the config-block comparison is what establishes this.

### The pre-registered prediction, graded against its own words

From `$WORK/run_jobBbc.sh`, written before the run:

> `n_below_floor` should FALL but not vanish, the surface drop should shrink by roughly 1.8 cm
> of 5.6, and `fz_over_analytic_measured` should move toward 1.0 without reaching it. If the
> leak is UNCHANGED the defect-2 hypothesis is refuted. If it vanishes entirely, the
> mass-deficiency story was wrong and that is more interesting still.

| limb of the prediction | predicted | measured | verdict |
|---|---|---|---|
| `n_below_floor` | falls, does not vanish | 26964 -> **1002**, **-96.3 %** | **CONFIRMED**, but see below |
| surface drop | shrinks by ~1.8 cm of 5.587 | 5.587 -> **3.262 cm**, shrink **2.325 cm** | **CONFIRMED**, but 1.29x the predicted 1.8, and see the offset below |
| `fz_over_analytic_measured` | moves toward 1.0, does not reach it | +50.06 % -> **+34.35 %** | **CONFIRMED** |

**All three limbs land on the predicted branch. The defect-2 hypothesis is CONFIRMED, not
refuted.** Two honesty qualifications added after review: **limb 3 is graded by hand, not by the
script** (`verdict_on_prediction()` grades only limbs 1 and 2), so the claim at the top of this
document that every number is script-regenerated does not cover it; and limb 2's acceptance band
was chosen inside the analysis script after the run, with 2.325 cm being **1.29x** the predicted
1.8 cm, which "slightly exceeds" understates.

**THE REPORTED SURFACE DROPS CARRY A 0.878 cm NON-PHYSICAL OFFSET.**
`sphere_heave.py:810` defines `surface_drop_m` against the DESIGN waterline 0.575 m, not the
frame-0 measured surface. Both runs measure 0.56622 m at frame 0, so **both report a 0.878 cm
"drop" before any physics happens**. The physical falls are:

| | reported vs design | frame-0 measured | **PHYSICAL fall** |
|---|---|---|---|
| control 918240 | 5.587 cm | 0.878 cm | **4.709 cm** |
| bcfix 918450 | 3.262 cm | 0.878 cm | **2.384 cm** |

**The 2.325 cm shrink is unaffected because the offset cancels**, so the headline survives. But
5.587 and 3.262 overstate the physical fall by 18.6 and 36.8 percent, and the pre-registered
prediction's own denominator "of 5.6" is inflated on the same basis.

**A useful by-product:** the two runs' frame-0 surfaces agree to 1e-6 m (0.566221 against
0.566222) against a particle half-spacing of 4.6875 mm. That is the evidence, which the config
comparison alone could not give, that both runs share the same `measure_surface` convention and
the "do not pool runs from before this commit" warning in `sphere_heave.py` does not bite here.

**And it is CONFIRMED harder than "partial" implied, which is the honest qualification.** The
surface-drop limb landed almost exactly where predicted, but the floor leak did not fall
partially, it fell by 96.3 percent. The full trajectory shows why that matters:

| frame | control below-floor | bcfix below-floor | control leaked % | bcfix leaked % |
|---|---|---|---|---|
| 0 | 2248 | **0** | 0.537 | 0.170 |
| 50 | 16610 | **0** | 4.597 | 1.931 |
| 100 | 22122 | 396 | 5.804 | 2.284 |
| 150 | 25301 | 921 | 6.506 | 2.615 |
| 199 | 26964 | 1002 | 6.915 | **2.819** |

**The floor leak is exactly zero for the first ~50 frames and only then re-forms slowly**, at
+85 over the last 50 frames against the control's +1693. So the strict inequality removed the
leak outright at the start; what survives is a slow re-formation. That is nearer the "vanishes
entirely" branch than the wording anticipated, without actually reaching it. Both readings
should be recorded, and the residual 1002 is what keeps the mass-deficiency story alive.

Two further measurements, neither of them predicted:

- **The wall leak got WORSE**, 14423 -> 15868, **+10.0 percent**, and it is now the dominant
  leak: 2.651 percent against the floor's 0.167. Total leakage still improved a lot,
  6.915 -> 2.819 percent. A plausible reading is that water no longer leaving through the floor
  has to go somewhere, but **the arithmetic mostly denies it**: the fix saved 25962 floor
  particles and the walls absorbed only 1445 of them, **5.6 percent**. The other 94.4 percent
  stayed in the tank. And at frame 0, before any bulk redirection is possible, the treatment
  already has MORE particles outside the walls (1016 against 965) while its floor count is 0
  against 2248. Treat the growth as unexplained, not as redirected water.
- **Occupied volume moves -1.082 percent over frames 50 to 199 in the control and +0.317
  percent in the treatment.** An earlier version read this as "the compaction signature
  reversed ... the treatment has neither [leakage nor compaction]". **That reading is withdrawn.**
  The treatment lost 2.819 percent of its particles, which is leakage by definition. And
  `occupied_volume_m3` is `occupied_cells * dx**3` (`sphere_heave.py:762-763`), a voxel count
  taken over ALL water particles including the 16870 that have escaped and now spread thinly
  below the floor and outside the walls, so it is confounded by escapee dispersal and cannot
  license a no-compaction claim.
- Lowest particle: **3.4 mm** below the floor plane, against the control's **22.6 mm**.

### THE GRADE. It is a FAIL, and it is window-robust

Graded on `fz_over_analytic_measured`, the accessor the source designates, against the manifest
bands (within 10 PASS, 10 to 25 REPORTABLE PARTIAL, beyond 25 FAIL):

| window | control 918240 | **bcfix 918450** |
|---|---|---|
| last 20 | +50.29 % FAIL | **+35.80 % FAIL** |
| last 40 | +49.86 % FAIL | **+35.38 % FAIL** |
| last 80 | +49.74 % FAIL | **+34.37 % FAIL** |
| last 100 | +50.06 % FAIL | **+34.35 % FAIL** |
| last 150 | +49.36 % FAIL | **+35.42 % FAIL** |
| last 200 | +49.43 % FAIL | **+36.40 % FAIL** |
| **across windows** | **FAIL, WINDOW-ROBUST** | **FAIL, WINDOW-ROBUST** |

**Two of those six rows are not late windows, which an earlier version did not say.** The series
is 200 frames, so "last 200" is the ENTIRE record including the startup transient and "last 150"
starts at frame 50. The control's first 20 frames read
`[-0.001, 0.101, 3.985, 3.549, 0.580, 0.000, 0.000, 0.016, 5.844, 1.281, ...]`. The FAIL
conclusion survives, because those excursions roughly cancel, but the rows must not be presented
as six equally-late windows. **One inference does NOT survive**: the control's nominal accessor
reaches PASS at last-200 only by 0.33 points (-9.67 percent), and dropping just frames 0 to 19
moves it to **-13.04 percent, a REPORTABLE PARTIAL**. So the "swings FAIL / PARTIAL / PASS"
characterisation below is partly manufactured by the transient.

Late-window blocked estimate, treatment: **1.343549 +/- 0.007031** (blocked SE, converged),
**+34.355 percent** from the closed form at the surface that exists.

**N IS 1 AGAINST 1. STATED HERE BECAUSE AN EARLIER VERSION OMITTED IT.** The A/B is one
control run against one treatment run, 200 frames each, with no repeat at either arm. These
runs are known non-deterministic: every ladder repeat in Part 1 is bit-distinct. **No repeat
exists for the sphere scene anywhere in the data**, so there is no measured bound on its
run-to-run spread. The blocked SEs quoted below (0.0075 and 0.0070) are WITHIN-run,
autocorrelation-corrected SEs of the mean and say nothing about between-run variance.

**The boundary fix moves the graded number from +50.06 to +34.35 percent and it is still a FAIL
at every window tested.** The DIRECTION is consistent across all six windows and is safe. **The
MAGNITUDE is not.** An earlier version called this "a real and large improvement of about 15.7
points"; that asserts a magnitude a single unreplicated pair cannot support, and 15.7 is only
the last-100 value (the six windows span 13.0 to 15.7 points). Correct form: **in one
unreplicated pair the graded ratio fell by 13.0 to 15.7 points depending on window, sign
consistent, magnitude not separated from run-to-run variation. Re-run with N >= 3 per arm
before quoting a number.**

**The one limb that DOES survive N=1 is the floor leak**, and for reasons that are categorical
rather than statistical: `n_below_floor` is exactly **0** at frames 0, 1, 2, 5, 10, 25 and 50,
it is 0 one tick in against the control's 2248, and the mechanism is a deterministic inclusion
test on an exactly-zero dot product. A 27x change against a hard zero is not repeat noise.
`R5_PHYSICS_BATCH_MANIFEST.md:214`: "Any FAIL stops the ladder." **The ladder remains stopped,
and Job C stays gated.**

This is NOT reported as NOT GRADEABLE. Manifest criterion 5 says a NOT-STATIONARY verdict here
is "expected, not disqualifying" and must be reported with its drift ratio, which is done below.

### A NEW FINDING: the two accessors now disagree in DIRECTION, and both are window-robust

| accessor | control | bcfix |
|---|---|---|
| `fz_over_analytic_measured` (designated) | +49 to +50 %, **FAIL, robust** | +34 to +36 %, **FAIL, robust** |
| `fz_over_analytic_nominal` | -29 to -10 %, **swings FAIL / PARTIAL / PASS** | -7.4 to +4.8 %, **PASS, but BAND-robust only** |

In the control the nominal accessor was simply non-diagnostic: it swung across all three bands
on window choice, which is the second defect recorded at handoff 3e. **In the treatment that
defect disappears** and the nominal accessor becomes a window-robust PASS. So the two accessors
now give opposite verdicts, and neither can be dismissed as an artifact of window choice.

The reason is measurable: **the free surface is still falling.** `surface_z_measured_m` has
`trend_stationary = False` at `slope_n_sigma = 16.90` in the late window (control: 19.98). The
measured accessor divides by the closed form at a denominator that is still shrinking, so it
stays high. The nominal accessor divides by the fixed design-waterline target, and the raw force
has risen to meet it.

**Grade on the designated accessor, so FAIL stands.** The designation exists precisely so that
"a falling free surface no longer masquerades as a coupling error" (`sphere_heave.py:804-805`),
and the surface is demonstrably still falling. But the nominal PASS is a real measurement and
should be reported beside it, not suppressed.

### RETRACTED: `grade_job_b.py` is NOT "wrong twice". Corrected 2026-08-18 after review

**An earlier version of this document claimed `grade_job_b.py` "grades the wrong series" and
that "the designated accessor is never reached", calling it a second, previously unrecorded
fault. THAT IS FALSE AND IS WITHDRAWN.** It was asserted from reading `:82` alone without
running the tool, which is the exact failure this project's claim discipline exists to stop.

Verified by execution, 2026-08-18, on the treatment JSON:

```
  BAND: NOT GRADEABLE
  reason: blocking.stationarity rejects this series (halves_stationary=False,
          trend_stationary=False, slope=-0.07190 N/frame at 3.95 sigma).
    measured-surface ratio 1.3435 (+34.35% from 1.0)  BAND: FAIL
```

**The grader DOES compute and band the designated accessor, on the refusal path, and returns
the identical +34.35 percent FAIL that this document's own script produces** (control: 1.5006,
+50.06 percent, FAIL). Further, `grade_job_b.py:199-205` records that this exact defect was
already found and already fixed: "this refusal path omitted the key entirely ... **It is
emitted on the refusal path too now.**" So the claim was neither new nor independent.

**The narrow, true, and much smaller defect** is that the **top-level `band` key** is populated
from the nominal path, so a machine reading `band` sees `NOT GRADEABLE` while the designated
accessor's FAIL sits one level down in `measured_surface_criterion`. That is a reporting-surface
problem, not a missing accessor. "Fix the accessor before Job C" was a misdiagnosis; the correct
action is to promote the measured criterion to the top-level band.

What survives unchanged: the refusal itself still rests on `fz_N`'s non-stationarity (8.52 and
3.95 sigma) while **the designated accessor is stationary in both runs** (0.64 and 1.08 sigma),
and manifest criterion 5 calls non-stationarity expected rather than disqualifying. So refusing
to return a top-level band is still the wrong behaviour. That was always the defensible half of
the claim and it is the only half retained.

**One caveat on that stationarity argument, which the earlier version overstated.** A stationary
ratio formed from two co-trending non-stationary series is not evidence the measurement has
settled; it is evidence numerator and denominator are falling together. The document's own
Part 2 already says this of the control. It applies to the treatment too.

Full stationarity readout, late window from frame 100:

| series | control | bcfix |
|---|---|---|
| `fz_N` | halves F, trend F, **8.52 sigma** | halves F, trend F, **3.95 sigma** |
| `fz_over_analytic_measured` | halves T, trend T, **0.64 sigma** | halves T, trend T, **1.08 sigma** |
| `surface_z_measured_m` | halves F, trend F, **19.98 sigma** | halves F, trend F, **16.90 sigma** |

### What this settles and what it does not

**Settles:** defect 2 is real and it was the dominant contributor to the floor leak. One
character of engine removes 96.3 percent of below-floor particles and 15.7 points of graded
error, at zero physics cost and with the pinned engine untouched.

**Does not settle:** Job B still FAILS, so the ladder stays stopped. The residual leak is now
mostly through the WALLS and it grew. The free surface is still falling at 16.90 sigma, so more
frames alone will not fix it.

**CORRECTED AFTER REVIEW: LEAKAGE DOES NOT ACCOUNT FOR THE SURFACE FALL, AND THE RESIDUAL FLOOR
LEAK IS TOO SMALL TO CARRY THE STORY.** Mass balance on the treatment, particle volume
`h**3` at `h = 0.009375` m over the 1.0 m x 1.0 m wall-to-wall footprint:

| term | value | share of the 2.384 cm physical fall |
|---|---|---|
| departed particles | 16870 = 2.819 % | |
| departed volume | 0.013900 m3 | |
| **surface fall from leakage** | **1.390 cm** | **58.3 %** |
| bounded compression, the source's own figure | 0.742 cm | 31.1 % |
| sum | 2.132 cm | vs 2.384 cm observed, residual 0.25 cm |

So **leakage explains at most 58 percent of the physical fall**, and only 43 percent of the
3.262 cm this document reports. The remainder closes with the compression term
`sphere_heave.py`'s own `water_budget` docstring already bounds: K = rho c^2 = 165000 Pa against
a mean hydrostatic rho g d/2 = 2448 Pa is a 1.4837 percent strain, shortening the column
0.742 cm and then stopping. The control closes the same way (3.410 + 0.742 = 4.152 against
4.709 observed).

**Consequently an earlier version's reasoning here was wrong.** It said "the mass-deficiency
hypothesis is not disposed of: a 1002-particle floor leak that re-forms after frame 50 is
exactly what a residual mass deficiency predicts". **The 1002-particle floor leak is 0.167
percent of the water and buys 0.014 cm of surface.** It cannot be the load-bearing term in a
2.384 cm fall. The load-bearing terms are the 15868-particle WALL leak and a bounded elastic
compression. The mass-deficiency hypothesis is not refuted either, but it must now be argued at
the WALLS, not the floor, and the sacrificial sub-floor `--ghost-layers` route (`6ed163e`,
handoff item 4) is aimed at the term that is now minor. **That is the single most decision-relevant
correction in this document.**

**Also note the two leak counters are NOT disjoint** (`sphere_heave.py:749-752`: a particle can
be below the floor AND outside the walls), so every "total leakage" figure here is an upper
bound. That makes the mass-balance shortfall above larger, not smaller.

Job `918461 d4_ghost` has since COMPLETED (00:02:14, ExitCode 0:0, both arms written, 300 frames
each) and is uncollected. It belongs to the D4 session, not this one.

---

## ENGINE TAGS AND THE COUPLING CAVEAT, both missing from the first version

**Every claim in this document is WARPMPM. No Genesis result appears anywhere.** But the two
parts use DIFFERENT coupling paths and the first version did not say so:

- **Part 1 is WARPMPM free-rigid, material-8**, via `renders/yaris_render_s1/sim_standing.py`.
  This is the lineage of the 17 canonical runs.
- **Part 2 is WARPMPM SDF-collider**, via the pinned `kernels/mpm_solver_warp.py`. The Job B
  JSON's own provenance block says so: "SDF collider, the path validated to 7.3-7.7% of analytic
  buoyancy; NOT the free-rigid material-8 path".

**THE J.1 CAVEAT APPLIES AND IS STATED HERE VERBATIM: none of Part 2 clears the 17 published
verdicts.** Three independent reasons, all checkable:

1. **Restitution differs.** `sphere_heave.py:577` and `:582` add the floor and walls with
   `restitution=0.0`; the 17 canonical runs use `restitution=0.05`
   (`sim_standing.py:210-214`).
2. **Depth resolution.** The canonical runs resolve the flow depth with 2 grid cells.
3. **Self-consistency is not validation.**

So the boundary fix graded in Part 2 is a result about the SDF-collider validation scene. It
does not transfer to Part 1's free-rigid ladder without a separate test, and no such test exists.

---

## PROVENANCE AND REPRODUCTION

| item | value |
|---|---|
| job 918351 | COMPLETED 00:10:50, ExitCode 0:0, `$WORK/r6_rep_g192_918351/rep_1..5/` |
| job 918450 | COMPLETED 00:06:53, ExitCode 0:0, `$WORK/d4_jobBbc_918450/sphere_fixed_bcfix.json` |
| control | job 918240, `$WORK/d4_jobB_918240/sphere_fixed_g64.json` |
| classifier | `simulation/failure_modes.py`, ssf 1.42, G 9.81, `sustain_frames` 3 (UNSOURCED) |
| accessor | `fz_over_analytic_measured`, `sphere_heave.py:804-805` and `:818` |
| bands | `docs/R5_PHYSICS_BATCH_MANIFEST.md` criterion 3 |
| `blocking.py` | `claude/r5-physics`, auto-located; not present on this branch |

**Two line-number corrections, both found by reading live rather than trusting the handoff:**

- The accessor designation is at **`sphere_heave.py:804-805`** (field at `:818`), **not
  `:669-670`** as the Round 7 handoff and the task text both state. The file was edited and the
  numbers drifted. CORRECTED after review: `:669` is `"mach_peak"` and `:670` is
  `"group_velocity_m_s"`; `measure_surface` begins at `:676`, so an earlier version of this
  document was itself wrong to say `:669-670` "lands in `measure_surface`'s docstring". The
  quoted sentence sits at `:806-807`; `:804-805` are the two comment lines preceding it.
- **`analysis/r6_repeat_stats.py` has no `GRIDS` dict at all.** The task text said its GRIDS
  dict "needs 192 adding"; that is a conflation with **`analysis/r6_hull_clearance.py:61`**,
  whose GRIDS dict is a dx lookup for the clearance rule and **already carries 192**
  (`0.049072`, which matches the measured `dx` of 0.0490715745506653 to five significant
  figures). Neither script computed the ladder margin table, which is why
  `analysis/r7_ladder_stats.py` was written.

**One citation carried, and it does NOT say what this document applies it to.** The
~10-particle-layers convention is attributed to Reis et al. 2021,
`10.1016/j.engstruct.2021.113280`, marked "verified matched" in the Round 7 handoff. Metadata
confirmed via Crossref (Reis, Clain, Figueiredo, Barbosa, Baptista, Lopes, *Engineering
Structures*, 2021-12). **Two problems, both flagged rather than resolved:**
(a) it is "Experimentally validated numerical models to assess tsunami hydrodynamic force on an
**elevated structure**", not a vehicle paper and not a resolution-convention paper; and
(b) `R6_A2_REPEATS_AND_JOBB_MEASURED_2026-08-18.md:367-368` states the convention as ~10 per
**wave height**, while this document and `r7_ladder_stats.py` apply it to **still-water flow
depth**. Those are different quantities. **The "g160 is the first grid to meet the literature
convention" framing rests on that substitution and should not be published until someone reads
the primary source.** UNREVIEWED against the primary text.

## REPRODUCIBILITY LIMIT, stated rather than left implicit

Part 2 is **not** reproducible from the artifacts in this repository alone. The three sha256
digests and both `sacct` results live on Vista, and this document itself concedes the control's
as-run scene file cannot be hash-compared. The JSONs are the reproducible part; the provenance
chain above them is attested, not verifiable here.

## ADVERSARIAL REVIEW, and what it changed

This document was checked by the `physics-skeptic` agent after the first version was committed
at `b6e4de3`. It returned **Not CLEAN with six blocking issues**. Every correction above was then
**re-verified independently by this session before being written**, not accepted on the agent's
report. What changed:

| # | claim in `b6e4de3` | status now |
|---|---|---|
| 1 | "`grade_job_b.py` is wrong twice" | **RETRACTED.** Running it emits the designated accessor with an identical FAIL; the source records the fault as already fixed |
| 2 | Job B N not stated | **N = 1 vs 1**, magnitude claim downgraded |
| 3 | 1002-particle floor leak carries the mass-deficiency story | **WRONG.** It is 0.167 % of the water and 0.014 cm of surface; leakage explains only 58 % of the fall |
| 4 | surface drops 5.587 / 3.262 cm | carry a **0.878 cm non-physical offset**; physical falls are 4.709 / 2.384 cm |
| 5 | "the treatment has neither leakage nor compaction" | **WITHDRAWN**, `occupied_volume_m3` cannot support it |
| 6 | g160/g192 STUCK is "a coherent picture" | **RECAST.** g160 drifts 5.5 cm and is STUCK only as a classifier artifact |
| 7 | confound is 1.15x | **1.37x by water volume**, plus a +17.5 % inflow-gap confound |
| 8 | no engine tag, no coupling caveat | **added** |

Two claims the agent raised that this session checked and did NOT adopt wholesale: the drift-table
sd values (recomputed here as population sd, means agree exactly), and the wall-leak reading
(kept as explicitly unexplained rather than replaced with a new mechanism).
