# Pane 0.7 Diagnosis + Full Gate Status — July 13, 1:51am checkpoint

## 0. Do not run any of the original sed/cp/mv commands yet

Every command that touched `can_it_ford_L2_mpm.py` tonight failed at "No such file or directory" before reaching any physics code. Running `sed`, `cp .bak`, or the combined-fix commands against a file that doesn't exist either does nothing (safe but useless) or, worse, could create a confusing empty/partial file depending on exact flags. Diagnose first.

---

## 1. Diagnostic sequence — run this instead, in order, in the pane that's actually on Vista

Confirm you're really where you think you are, and see what's actually in the directory:
```bash
pwd
ls -la
```

Search broadly across all three Vista storage tiers — don't assume the file is only missing from one location:
```bash
find /work/11603/jcerrell0629 -iname "can_it_ford_L2_mpm*" 2>/dev/null
find $HOME -iname "can_it_ford_L2_mpm*" 2>/dev/null
find $SCRATCH -iname "can_it_ford_L2_mpm*" 2>/dev/null
```

If none of those find it, check whether it was ever actually committed to git at this path, or whether it only ever existed as an uncommitted local file that a `git pull --rebase` or checkout could have discarded:
```bash
cd /work/11603/jcerrell0629/vista/can-it-ford
git status
git log --all --oneline -- "*can_it_ford_L2_mpm*"
git log --oneline -5
git remote -v
git branch --show-current
```

If git shows it was committed somewhere but HEAD doesn't have it, check whether you're just behind:
```bash
git fetch origin
git log HEAD..origin/main --oneline
```
If commits show up here, `git pull --rebase` should bring the file back — that's the boring, expected case, not a disaster.

Cross-check the Mac side — the authoritative copy may live there if it needs re-pushing rather than reconstructing from scratch. Open a **separate terminal, not inside any Vista SSH session:**
```bash
find ~/can-it-ford -iname "can_it_ford_L2_mpm*"
cd ~/can-it-ford && git log --all --oneline -- "*can_it_ford_L2_mpm*"
```

Also worth a Spotlight sweep, given this project's documented history of stray duplicate folders (`Desktop/NEW_FORD_FILES`, `Downloads/NEW_FORD_FILES`, per the July 7 duplicate audit):
```bash
mdfind -name "can_it_ford_L2_mpm.py"
```

**Decision point:**
- Found in git history (Vista or Mac) but not on disk → `git pull --rebase` or `git checkout <commit> -- <path>`, don't retype it by hand.
- Found as a stray file on the Mac, never committed → `scp` it to Vista, then `git add`/commit it properly so this can't happen again.
- Genuinely nowhere → I have the exact fragments quoted verbatim from last night's session (the argparse before/after diff, the `rho=604` line, the water/vehicle `pos`/`size` tuples) — reconstructable as a last resort, tell me and I'll write the full file from those verified fragments rather than guessing.

## 2. Fix the second pane (never actually connected to Vista)

```bash
ssh jcerrell0629@vista.tacc.utexas.edu
idev -p gh-dev -N 1 -n 1 -t 00:30:00
module load tacc-apptainer
export GENESIS_PATH=/work/10386/lsmith9003/vista/containers/genesis_container.sif
cd /work/11603/jcerrell0629/vista/can-it-ford
```
Only once this shows a Vista compute-node prompt (`c###-###[gh][...]$`, not your Mac's `josie@Josephines-MacBook-Air`) does it make sense to run any of the crash-test sequence in it.

## 3. Gate status for everything else, given tonight's screenshot

| Pane / task | Status | Action |
|---|---|---|
| Crash test (Pane 0.7) | **Blocked** — file not found, root cause not yet known | Run §1 above first |
| Second Vista-command pane | **Blocked** — never SSH'd in | Run §2 above first |
| FloodScene sweep (Track 1) | **Working** — real yaw/roll/disp values, not blocked by any of the above | Keep going, unrelated repo/system |
| Stranded CSV pulls (d0p22, d0p38, d0p30_check) | **Done** | No action needed |
| Al-Qadami citation grep on Vista | **Ran, correctly empty** — that citation lives in a local project file, not `citations/*.md` on Vista, confirmed last session | Not a new bug, don't re-run this |
| Kumar update draft | **Correctly held, not sent** | Keep holding — still gated on Pane 0.7 *and* the full-scale FloodScene run both landing |
| STATUS.md consolidated write (Pane 0.4) | **Still gated** | Wait on §1's outcome before writing anything definitive about the crash |
| Everything else from the prior file (bridge/ scaffold check, DRIFT_THRESHOLD citation fix, local conda/zshrc cleanup) | **Untouched, fully independent, safe to start now** | No dependency on tonight's file mystery at all |

Nothing here invalidates the original 8-pane plan — it just inserts one diagnostic step before Pane 0.7 specifically, and fixes one pane's SSH state. Everything else in `2026-07-13_bug_triage_and_panel_execution_plan.md` still applies as written.

---

## 4. RESOLVED, 2:05am — real path found

```
/work/11603/jcerrell0629/vista/can-it-ford/simulation/can_it_ford_L2_mpm.py
/work/11603/jcerrell0629/vista/can-it-ford/simulation/can_it_ford_L2_mpm_ytest.py
/work/11603/jcerrell0629/vista/can-it-ford/simulation/can_it_ford_L2_mpm.py.bak2
/work/11603/jcerrell0629/vista/can-it-ford/simulation/can_it_ford_L2_mpm.py.bak4
/work/11603/jcerrell0629/vista/can-it-ford/simulation/can_it_ford_L2_mpm.py.bak5
```

Not a lost file — a stale path assumption. `CLAUDE.md`'s "RESOLVED July 7: flat layout, no `simulation/` subdirectory" note is now out of date; the repo has since been restructured to match the Mac/GitHub canonical layout (`simulation/` subdirectory, per `PROJECT_FILE_MAP.md`). This is exactly the ambiguity Part 3b of the master doc already flagged as an open risk ("flat-layout vs `simulation/` subdirectory... prior sessions found inconsistency here") — now confirmed which way it resolved. Update that line in `CLAUDE.md` so this doesn't repeat.

**Pre-existing `.bak2`/`.bak4`/`.bak5` files mean this may not be the first isolation attempt.** Diff them against the current file and each other before running anything new — one of them may already answer the mass/overlap question.

### Corrected Pane 0.7 sequence — run from repo root, every path now includes `simulation/`

```bash
cd /work/11603/jcerrell0629/vista/can-it-ford
ls -la simulation/can_it_ford_L2_mpm*.py simulation/*.bak*
diff simulation/can_it_ford_L2_mpm.py simulation/can_it_ford_L2_mpm.py.bak2
diff simulation/can_it_ford_L2_mpm.py simulation/can_it_ford_L2_mpm.py.bak4
diff simulation/can_it_ford_L2_mpm.py simulation/can_it_ford_L2_mpm.py.bak5
```
Check these first. A zero or near-zero diff on any of them (e.g. only the `rho=` line differs) means someone already ran this exact test.

```bash
grep -n "rho=" simulation/can_it_ford_L2_mpm.py
grep -n "pos=(1.0, 0.0, 0.755)" simulation/can_it_ford_L2_mpm.py
grep -n "pos=(0.275" simulation/can_it_ford_L2_mpm.py
mkdir -p logs
CUDA_LAUNCH_BLOCKING=1 apptainer exec --nv $GENESIS_PATH python3 simulation/can_it_ford_L2_mpm.py --depth 0.30 --velocity 0.0 2>&1 | tee logs/mpm_crash_baseline_july13.txt
```

```bash
cp simulation/can_it_ford_L2_mpm.py simulation/can_it_ford_L2_mpm_test_overlap.py
sed -i 's/pos=(1.0, 0.0, 0.755)/pos=(6.0, 0.0, 0.755)/' simulation/can_it_ford_L2_mpm_test_overlap.py
CUDA_LAUNCH_BLOCKING=1 apptainer exec --nv $GENESIS_PATH python3 simulation/can_it_ford_L2_mpm_test_overlap.py --depth 0.30 --velocity 0.0 2>&1 | tee logs/mpm_test_overlap_only_july13.txt
```

```bash
cp simulation/can_it_ford_L2_mpm.py simulation/can_it_ford_L2_mpm_test_mass.py
sed -i 's/rho=604/rho=115.7/' simulation/can_it_ford_L2_mpm_test_mass.py
CUDA_LAUNCH_BLOCKING=1 apptainer exec --nv $GENESIS_PATH python3 simulation/can_it_ford_L2_mpm_test_mass.py --depth 0.30 --velocity 0.0 2>&1 | tee logs/mpm_test_mass_only_july13.txt
```

```bash
cp simulation/can_it_ford_L2_mpm.py simulation/can_it_ford_L2_mpm.py.bak_july13
sed -i 's/rho=604/rho=115.7/' simulation/can_it_ford_L2_mpm.py
CUDA_LAUNCH_BLOCKING=1 apptainer exec --nv $GENESIS_PATH python3 simulation/can_it_ford_L2_mpm.py --depth 0.30 --velocity 0.0 2>&1 | tee logs/mpm_test_combined_july13.txt
CUDA_LAUNCH_BLOCKING=1 apptainer exec --nv $GENESIS_PATH python3 simulation/can_it_ford_L2_mpm.py --depth 0.60 --velocity 2.0 2>&1 | tee logs/mpm_test_combined_hard_july13.txt
```

```bash
diff simulation/can_it_ford_L2_mpm.py simulation/can_it_ford_L2_mpm_ytest.py | head -60
cat > logs/pane0.7_result.md << 'EOF2'
Path resolved: simulation/can_it_ford_L2_mpm.py
Pre-existing .bak2/.bak4/.bak5 findings: ___
Baseline: CRASHED / DID NOT CRASH
Overlap-fix only: CRASHED / DID NOT CRASH
Mass-fix only: CRASHED / DID NOT CRASH
Combined (0.30/0.0 and 0.60/2.0): CRASHED / DID NOT CRASH
EOF2
```

---

## 5. UPDATED, 2:14am — full commit history changes the approach

`echo $SCRATCH` confirmed a real path (`/scratch/11603/jcerrell0629`); the repeated `find` against it still returned nothing. `$SCRATCH` is closed as a location for this file.

`git status` on `/work/11603/jcerrell0629/vista/can-it-ford` shows `simulation/can_it_ford_L2_mpm.py.bak2/.bak4/.bak5` as **untracked** — confirmed never committed, won't appear in git log, only in a direct file diff.

The real find: `git log --all --oneline -5` plus a file-history search surfaced the **actual prior history** of `can_it_ford_L2_mpm.py`:

| Commit | What it did |
|---|---|
| `45c786b` | File created — draft MPM migration, untested |
| `3a306ad` | Synced coup_friction/substeps to SPH script's validated values |
| `1d5ad45` | **Domain-padding fix** — extended domain past water/vehicle geometry to absorb Genesis's 3-grid-cell safety padding (0.046875m at grid_density=64) |
| `87c1bda` | grid_density 64→128, testing as a CUDA crash fix |
| `5d23037` | coup_friction aligned to Azhar et al. 2023's exact 0.55 |
| `8d1ea39` | **"Re-apply Genesis L2 MPM fixes lost in rebase conflict resolution: substeps 32, grid_density 64, water box overlap fix, floor clearance, inflow mask fix"** — the overlap fix already existed and was restored here after being lost |
| `7e5f9f4` | run_tag renamed to reflect grid_density=64 (implies it had reverted from 128 back to 64 again) |
| `7f7fdb2` | grid_density 64→128 again, per Genesis issue #600 |
| `67915be` | Sedan-scale resize — **no mention of re-deriving overlap/floor-clearance for the new box dimensions** |
| `69d27af` | HEAD — argparse fix |

**Working theory, stronger than before:** the overlap fix from `8d1ea39` was real and worked for the *old* box size. `67915be`'s resize to the real sedan bbox almost certainly invalidated whatever floor-clearance/overlap logic `8d1ea39` set up, without anyone re-deriving it for the new dimensions. `grid_density` has also flip-flopped 64↔128 across at least three commits — don't trust either value without checking live.

### Commands to run before the sed-based isolation tests

```bash
cd /work/11603/jcerrell0629/vista/can-it-ford
grep -n "grid_density" simulation/can_it_ford_L2_mpm.py
```
Check which value is actually live right now — don't assume 128 from the prior plan.

```bash
git log --all --follow --oneline -- "**/can_it_ford_L2_mpm.py" can_it_ford_L2_mpm.py
```
`--follow` and both path forms, in case commits before the `simulation/` reorg (dated Jul 12 19:25, per the directory mtime) reference the file at a different path — a plain pathspec filter can silently miss pre-reorg commits.

```bash
git show 8d1ea39 -- simulation/can_it_ford_L2_mpm.py 2>/dev/null || git show 8d1ea39 -- can_it_ford_L2_mpm.py 2>/dev/null
```
See exactly what the "water box overlap fix, floor clearance" logic looked like — this may be a reusable formula (e.g. vehicle z derived from box half-height + water depth), not something to invent fresh.

```bash
git show 1d5ad45 -- simulation/can_it_ford_L2_mpm.py 2>/dev/null || git show 1d5ad45 -- can_it_ford_L2_mpm.py 2>/dev/null
```
See the domain-padding fix — a third candidate contributor if overlap-fix-alone and mass-fix-alone both fail to stop the crash.

```bash
git show 67915be -- simulation/can_it_ford_L2_mpm.py 2>/dev/null || git show 67915be -- can_it_ford_L2_mpm.py 2>/dev/null
```
Specifically look for whether this diff overwrote a computed z-position with a hardcoded `0.755` — this would directly confirm the regression theory rather than leaving it as a guess.

```bash
diff simulation/can_it_ford_L2_mpm.py simulation/can_it_ford_L2_mpm.py.bak2
diff simulation/can_it_ford_L2_mpm.py simulation/can_it_ford_L2_mpm.py.bak4
diff simulation/can_it_ford_L2_mpm.py simulation/can_it_ford_L2_mpm.py.bak5
```
Still worth running — these are untracked, so they're not in any of the git output above and could be more recent than `69d27af`.

**Only after these** does the sed-based overlap/mass isolation sequence from §4 make sense to run — and if `git show 8d1ea39` reveals a real formula, use that instead of the flat `pos=(6.0, 0.0, 0.755)` displacement hack for the overlap test.
